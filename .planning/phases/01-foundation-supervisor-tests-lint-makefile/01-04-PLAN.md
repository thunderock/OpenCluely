---
phase: 01-foundation-supervisor-tests-lint-makefile
plan: 4
type: execute
wave: 2
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - package.json
  - package-lock.json
  - eslint.config.js
  - onboarding.js
  - main.js
autonomous: true

must_haves:
  truths:
    - "eslint@^9 and globals are devDependencies and package-lock.json is regenerated in sync (npm ci would succeed)"
    - "eslint.config.js is a flat CommonJS config with three files-scoped blocks (node/main+preload+scripts+tests, renderer/browser) plus global ignores for vendored/generated dirs, error-only, no-undef + a lenient no-unused-vars"
    - "npx eslint . exits 0 (clean) across the whole repo, including every file created in wave 1"
    - "no mass reformat occurred — only lint errors were fixed surgically (per-layer indentation/quote style preserved)"
  artifacts:
    - path: eslint.config.js
      provides: "Flat ESLint 9 config with per-environment globals and a lean error-only ruleset"
      contains: "sourceType"
      min_lines: 30
    - path: package.json
      provides: "eslint + globals declared as devDependencies"
      contains: "eslint"
  key_links:
    - from: eslint.config.js
      to: globals
      via: "languageOptions.globals = globals.node / globals.browser"
      pattern: "globals\\.(node|browser)"
    - from: package.json
      to: package-lock.json
      via: "lockfile regenerated after adding devDeps (npm ci parity)"
      pattern: "eslint"
---

<objective>
Add ESLint 9 (flat config) as the repo's lint gate: install `eslint@^9` + `globals` as devDependencies (regenerating the committed lockfile), author a lean, error-only `eslint.config.js` with correct per-environment globals for the three layers (main/Node, preload, renderer/browser), then run the fix pass so `npx eslint .` is clean across the whole repo — including every file created in Wave 1. No mass reformat; fix only the handful of real errors ESLint surfaces. This delivers the lint half of FND-02.

Purpose: A clean, gating lint config is the prerequisite for the CI lint gate (01-05) and for safely refactoring the god-files in later phases.
Output: updated `package.json` + `package-lock.json`, a new `eslint.config.js`, and a clean `npx eslint .`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-supervisor-tests-lint-makefile/01-RESEARCH.md
@.planning/phases/01-foundation-supervisor-tests-lint-makefile/01-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

@package.json
@onboarding.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install ESLint + globals and author eslint.config.js</name>
  <files>package.json, package-lock.json, eslint.config.js</files>
  <action>
Install the ONLY new dependencies this phase adds (both dev): `npm install --save-dev eslint@^9 globals`. Pin `eslint@^9` (not 10) for maximal Node-20 safety in CI, per RESEARCH Open Question 1. This regenerates `package-lock.json` — BOTH `package.json` and `package-lock.json` must be committed together or CI `npm ci` fails hard (Pitfall 4). Do NOT add `@eslint/js` or any stylistic plugin (the lean hand-rolled ruleset needs neither).

Author `eslint.config.js` at the repo root as a flat, CommonJS config (`module.exports = [ ... ]`) following the RESEARCH "eslint.config.js" example. Requirements:
- `const globals = require('globals');` at the top.
- A shared `leanRules` object: `'no-undef': 'error'` and `'no-unused-vars': ['error', { args: 'after-used', argsIgnorePattern: '^_', varsIgnorePattern: '^_', caughtErrorsIgnorePattern: '^_' }]` (the `caughtErrorsIgnorePattern` preserves the repo's `catch (_) {}` idiom — Pitfall 3).
- Block 0 — global `ignores`: `node_modules/**`, `dist/**`, `.venv-whisper/**`, `.whisper-models/**`, `lib/markdown.js` (vendored 1725-line parser), `assests/vendor/**` (vendored Font Awesome — keep the intentional misspelling), `webapp/**` (standalone marketing site).
- Block 1 — Node/CommonJS layer: `files: ['main.js','preload.js','prompt-loader.js','speech-recognition.js','src/core/**/*.js','src/managers/**/*.js','src/services/**/*.js','scripts/**/*.js','test/**/*.js','eslint.config.js','tailwind.config.js']`, `languageOptions: { ecmaVersion: 2022, sourceType: 'commonjs', globals: { ...globals.node } }`, `rules: leanRules`. (`sourceType: 'commonjs'` is essential — the flat-config default is `'module'`, which misparses `require`/`module`. `test/**/*.js` covers both `*.test.js` and the fixtures dir.)
- Block 2 — renderer/browser layer: `files: ['src/ui/**/*.js','onboarding.js']`, `languageOptions: { ecmaVersion: 2022, sourceType: 'script', globals: { ...globals.browser, electronAPI: 'readonly', api: 'readonly', markdown: 'readonly', marked: 'readonly', renderMathInElement: 'readonly', Prism: 'readonly', mainWindowUI: 'writable' } }`, `rules: leanRules`. (These app-injected globals are referenced in renderer files; declaring them prevents false `no-undef` — Pitfall 7.)

Do NOT run `--fix` (no mass reformat). The exact rule list beyond the two named rules is Claude's discretion (per CONTEXT) — start with just `no-undef` + `no-unused-vars`.
  </action>
  <verify>`npx eslint --version` prints a 9.x version; `node --check eslint.config.js` exits 0; `npm ci --dry-run` (or inspect that `package-lock.json` now contains `eslint`) confirms lockfile/manifest parity; `node -e "require('globals')"` succeeds.</verify>
  <done>eslint + globals are committed devDeps with a regenerated lockfile; eslint.config.js is a valid flat CommonJS config with the two Node/renderer file blocks, correct per-env globals, global ignores, and the lean error-only ruleset.</done>
</task>

<task type="auto">
  <name>Task 2: Run the lint fix pass until `npx eslint .` is clean</name>
  <files>onboarding.js, main.js</files>
  <action>
Run `npx eslint .` and fix ONLY the errors it surfaces — surgically, no reformat, preserving each layer's indentation (2-space main / 4-space renderer) and its quote style. Expected handful (per RESEARCH pitfalls 3 & 7 and the codebase intel):
- `onboarding.js:1` has a relic `/* eslint-disable no-undef */`. Once the renderer block declares the browser + app-injected globals, this directive is unnecessary and ESLint 9 reports it as an unused disable directive. REMOVE the directive line (cleanest).
- Unused function args flagged by `no-unused-vars` with `args:'after-used'` — this only flags args at/after the last USED arg, so most `(event, x) => use(x)` handlers are FINE. For the genuinely-unused trailing ones (e.g. a bare `(event) => ...` where `event` is unused, in `main.js` IPC handlers), rename to `_event` (matches `argsIgnorePattern:'^_'`) — do NOT delete the arg if the handler signature is dictated by the framework.
- Any real `no-undef` in a renderer file for an app-injected global not yet listed: add it to the renderer block's `globals` (Block 2) rather than disabling the rule.

If the pass surfaces many unused `catch (e)` bindings (churn beyond "a handful"), you may switch `caughtErrors` to `'none'` in `leanRules` instead of renaming — Claude's discretion per CONTEXT (exact rule list). If ESLint flags a genuine bug (e.g. `no-dupe-keys`, `no-unreachable`) you may add that specific real-bug rule and fix the code; do NOT add stylistic rules or reformat. Iterate `npx eslint .` until it exits 0.

Every file created in Wave 1 (`src/core/env-file.js`, `skill-normalizer.js`, `vad-segmenter.js`, `service-supervisor.js`, the `test/*.test.js` files, `test/fixtures/dummy-service.js`) is now part of the lint scope — they were authored clean, but this pass is the backstop that guarantees the whole repo is green.
  </action>
  <verify>`npx eslint .` exits 0 with no output; `git diff --stat` shows only surgical edits (removed directive, `_event` renames, config tweaks) — NOT wholesale reformatting of any file; `grep -n "eslint-disable no-undef" onboarding.js` returns nothing.</verify>
  <done>`npx eslint .` is clean across the entire repo under the error-only gate, achieved by fixing the handful of surfaced errors without any mass reformat.</done>
</task>

</tasks>

<verification>
- `npx eslint .` exits 0 (this is the gate the Makefile and CI will invoke in 01-05).
- `package.json` + `package-lock.json` are in sync (a subsequent `npm ci` would succeed) and both include `eslint`/`globals`.
- The lint pass preserved per-layer style — no file was reindented or re-quoted en masse (locked: "no mass reformat, gate on errors only").
- The renderer block parses classic scripts (`sourceType:'script'`) with browser + app-injected globals; the Node block parses CommonJS with node globals.
</verification>

<success_criteria>
The lint half of FND-02 is delivered: a lean, error-only ESLint 9 flat config with correct per-environment globals, installed as a committed devDependency with a synced lockfile, and a repo that passes `npx eslint .` cleanly — ready to be gated by the Makefile and CI in 01-05.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-supervisor-tests-lint-makefile/01-04-SUMMARY.md` using the summary template.
</output>
