---
phase: 01-foundation-supervisor-tests-lint-makefile
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/env-file.js
  - src/core/skill-normalizer.js
  - main.js
  - src/core/first-run.js
  - prompt-loader.js
  - test/env-file.test.js
  - test/skill-normalizer.test.js
autonomous: true

must_haves:
  truths:
    - "env-file.js exposes parseEnv / formatEnvValue / upsertEnvContent as pure functions (string in, string/object out; no fs, no process.env, no config)"
    - "skill-normalizer.js exposes normalizeSkillName / injectProgrammingLanguage / SKILLS_REQUIRING_PROGRAMMING_LANGUAGE as pure exports (no fs)"
    - "main.js persistEnvUpdates and first-run.js _readEnv delegate to env-file.js with unchanged observable behavior"
    - "prompt-loader.js normalizeSkillName / injectProgrammingLanguage delegate to skill-normalizer.js with unchanged observable behavior"
    - "node --test test/env-file.test.js and test/skill-normalizer.test.js pass, requiring ONLY the extracted modules (never main.js/first-run.js/prompt-loader.js singletons)"
  artifacts:
    - path: src/core/env-file.js
      provides: "Pure .env parse/format/upsert helpers extracted from main.js + first-run.js"
      exports: ["parseEnv", "formatEnvValue", "upsertEnvContent"]
      min_lines: 40
    - path: src/core/skill-normalizer.js
      provides: "Pure skill-name normalization + programming-language injection extracted from prompt-loader.js"
      exports: ["normalizeSkillName", "injectProgrammingLanguage", "SKILLS_REQUIRING_PROGRAMMING_LANGUAGE"]
      min_lines: 40
    - path: test/env-file.test.js
      provides: "node:test coverage of the .env parse/format/upsert round-trip"
      contains: "require('../src/core/env-file')"
    - path: test/skill-normalizer.test.js
      provides: "node:test coverage of skill normalization + language injection"
      contains: "require('../src/core/skill-normalizer')"
  key_links:
    - from: main.js
      to: src/core/env-file.js
      via: "require + upsertEnvContent/formatEnvValue in persistEnvUpdates"
      pattern: "require\\(\"\\./src/core/env-file\"\\)"
    - from: src/core/first-run.js
      to: src/core/env-file.js
      via: "parseEnv in _readEnv"
      pattern: "parseEnv\\("
    - from: prompt-loader.js
      to: src/core/skill-normalizer.js
      via: "delegation in normalizeSkillName/injectProgrammingLanguage"
      pattern: "require\\('\\./src/core/skill-normalizer'\\)"
---

<objective>
Extract the `.env` string round-trip (from `main.js` + `src/core/first-run.js`) and the skill-name/prompt normalization (from `prompt-loader.js`) into two pure, dependency-free modules under `src/core/`, have the existing singletons delegate to them with ZERO behavior change, and pin each with a `node:test` suite. This delivers two of the three FND-01 pure-logic test targets (`.env` parse, skill/prompt normalization).

Purpose: These are the two lowest-risk extractions (small pure string transforms trapped behind `fs`). Getting them tested first proves the extract-and-delegate pattern before the harder VAD extraction (01-02).
Output: `src/core/env-file.js`, `src/core/skill-normalizer.js`, delegating edits to 3 existing files, and 2 passing test files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-supervisor-tests-lint-makefile/01-RESEARCH.md
@.planning/phases/01-foundation-supervisor-tests-lint-makefile/01-CONTEXT.md
@.planning/codebase/CONVENTIONS.md

# Extraction sources (verify line numbers before editing — they were current at planning time)
@main.js
@src/core/first-run.js
@prompt-loader.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract env-file.js and delegate main.js + first-run.js</name>
  <files>src/core/env-file.js, main.js, src/core/first-run.js, test/env-file.test.js</files>
  <action>
Create `src/core/env-file.js` (src/core style: 2-space indent, single-quote require/strings). Add a short top-of-file comment: pure `.env` helpers extracted from main.js + first-run.js; no fs / process.env / config so they are unit-testable without the Electron app. Export three functions:

1. `formatEnvValue(raw)` — copy VERBATIM from `main.js:34-40` (the newline-collapse + selective single/double-quote-wrap logic). Preserve every string LITERAL exactly (the `'${v}'` single-quote wrap and the `"..."` double-quote fallback are SEMANTIC dotenv behavior, not style — do not change them). Only the surrounding JS may follow src/core style.
2. `parseEnv(content)` — the parse loop from `first-run.js:97-121`, refactored to take a content STRING (not read a file) and return the key/value object. Preserve the quoted-value handling and the unquoted trailing `" #"` inline-comment stripping exactly.
3. `upsertEnvContent(existing, updates)` — the line-rewrite logic from `main.js:1678-1708`: split `existing` into lines, replace any line matching `/^\s*([A-Z0-9_]+)\s*=/` whose key is in `updates` (value via `formatEnvValue`), append keys not already present, and RETURN the joined string (`\n`-joined). This is pure — it does NOT touch `process.env` and does NOT write files.

`module.exports = { parseEnv, formatEnvValue, upsertEnvContent };`

Then delegate (behavior must stay identical):
- `main.js`: DELETE the local `formatEnvValue` definition (34-40). Add near the top requires (match main.js double-quote style): `const { formatEnvValue, upsertEnvContent } = require("./src/core/env-file");` — env-file.js is side-effect-free so require order does not matter. Rewrite `persistEnvUpdates` (1659-1723): keep the `updates` guard, `ENV_PATH`, the try/catch `fs.readFileSync` (default `""`), then `const content = upsertEnvContent(existing, updates);`, keep the `for (const key of keys) process.env[key] = String(updates[key]);` loop, keep the atomic temp-file+rename write of `content`, keep the logger calls, and `return keys;` (every provided key is always upserted, so this is the same membership the original `Array.from(updated)` returned). `formatEnvValue` is still referenced by env-file.js only — confirm main.js no longer defines it.
- `src/core/first-run.js`: add `const { parseEnv } = require('./env-file');` at the top. Replace the body of `_readEnv` (93-126) with: `try { return parseEnv(fs.readFileSync(this.envPath, 'utf8')); } catch (_) { return {}; }`.

Create `test/env-file.test.js` using `node:test` + `node:assert/strict`, requiring ONLY `../src/core/env-file`. Cover:
- formatEnvValue: simple value stays unquoted (`formatEnvValue('turbo') === 'turbo'`); whitespace wraps in single quotes (`formatEnvValue('a b') === "'a b'"`); Windows backslashes preserved (`formatEnvValue('C:\\Users\\Jane Doe\\python.exe')` keeps the backslashes, single-quoted); newlines collapse to a space; a value containing a single quote falls back to double quotes.
- parseEnv: skips blank + `#` comment lines; reads `KEY=value`; unwraps quoted values; strips a trailing unquoted ` #` inline comment; handles CRLF (`\r\n`).
- upsertEnvContent: replaces an existing `KEY=` in place while preserving a leading comment line; appends a genuinely new key; leaves unrelated lines untouched.
  </action>
  <verify>`node --check src/core/env-file.js && node --check main.js && node --check src/core/first-run.js` all exit 0; `node --test test/env-file.test.js` passes; `grep -n "function formatEnvValue" main.js` returns nothing (local def removed); `grep -n "require(\"./src/core/env-file\")" main.js` matches.</verify>
  <done>env-file.js exports the 3 pure functions; main.js persistEnvUpdates + first-run.js _readEnv delegate to it; the env-file test suite passes requiring only the extracted module.</done>
</task>

<task type="auto">
  <name>Task 2: Extract skill-normalizer.js and delegate prompt-loader.js</name>
  <files>src/core/skill-normalizer.js, prompt-loader.js, test/skill-normalizer.test.js</files>
  <action>
Create `src/core/skill-normalizer.js` (src/core style: 2-space, single quotes). Comment: pure skill normalization + language injection extracted from prompt-loader.js; the fs-backed `loadPrompts`/`getSkillPrompt` stay in `PromptLoader`, which delegates the pure bits here. Export:

1. `SKILLS_REQUIRING_PROGRAMMING_LANGUAGE = ['dsa']` (the single source of truth for the list currently hardcoded at `prompt-loader.js:10`).
2. `normalizeSkillName(skillName)` — copy VERBATIM from `prompt-loader.js:319-361` (the null→'general' guard, lowercase/trim, the full `skillMap`, and the `skillMap[normalized] || normalized` return).
3. `injectProgrammingLanguage(promptContent, programmingLanguage, skillName)` — copy VERBATIM from `prompt-loader.js:80-106` (the `languageMap`/`fenceTagMap`, the `dsa` switch case with its exact multi-line template, and the default case). Preserve the template strings byte-for-byte (they are prompt content).

`module.exports = { normalizeSkillName, injectProgrammingLanguage, SKILLS_REQUIRING_PROGRAMMING_LANGUAGE };`

Then delegate in `prompt-loader.js` (do NOT change the `module.exports = { PromptLoader, promptLoader }` shape at 405-408):
- Add at top: `const skillNormalizer = require('./src/core/skill-normalizer');`
- Constructor (line 10): `this.skillsRequiringProgrammingLanguage = [...skillNormalizer.SKILLS_REQUIRING_PROGRAMMING_LANGUAGE];`
- Method `normalizeSkillName(skillName)` (319-361): replace body with `return skillNormalizer.normalizeSkillName(skillName);`
- Method `injectProgrammingLanguage(promptContent, programmingLanguage, skillName)` (80-106): replace body with `return skillNormalizer.injectProgrammingLanguage(promptContent, programmingLanguage, skillName);`
- Leave all other methods (`getSkillPrompt`, `loadPrompts`, `getAvailableSkills`, `requiresProgrammingLanguage`, etc.) untouched — they call the (now-delegating) methods, so behavior is unchanged.

Create `test/skill-normalizer.test.js` (`node:test` + `node:assert/strict`), requiring ONLY `../src/core/skill-normalizer`. Cover:
- normalizeSkillName: `'DSA'`→`'dsa'`, `'algorithms'`→`'dsa'`, `'data-structures'`→`'dsa'`, `'ml'`→`'data-science'`, an unknown skill like `'  Rust  '`→`'rust'` (lowercased+trimmed passthrough), and `''`/`null`/`undefined`→`'general'`.
- injectProgrammingLanguage: for skill `'dsa'` with `'cpp'` the result ends with the DSA block and contains `IMPLEMENTATION LANGUAGE: C++` and a ```cpp fence tag; for `'dsa'` with `'js'` contains `JavaScript` and ```javascript; for a non-dsa skill (default case) contains `PROGRAMMING LANGUAGE:`; an unknown language like `'rust'` is title-cased (`Rust`).
- `assert.deepStrictEqual(SKILLS_REQUIRING_PROGRAMMING_LANGUAGE, ['dsa'])`.
  </action>
  <verify>`node --check src/core/skill-normalizer.js && node --check prompt-loader.js` exit 0; `node --test test/skill-normalizer.test.js` passes; `grep -n "require('./src/core/skill-normalizer')" prompt-loader.js` matches.</verify>
  <done>skill-normalizer.js exports the 3 pure symbols; prompt-loader.js delegates without changing its export shape; the skill-normalizer test passes requiring only the extracted module.</done>
</task>

</tasks>

<verification>
- `node --test test/*.test.js` runs both new files with zero failures and does NOT hang (no fixture under `test/` yet).
- All five touched/created `.js` files pass `node --check`.
- Tests import the extracted `src/core/*` modules only — grep the test files for `speech.service`, `first-run`, `prompt-loader`, `main` and confirm NO such requires (per the quality gate: tests require the extracted modules, never the singletons).
- Author every new/edited line lint-clean in anticipation of 01-04: no undeclared globals; no unused vars (use `_`-prefixed names or `catch (_)` for intentionally-unused); 2-space single-quote src/core style for the new modules.
</verification>

<success_criteria>
Two of the three FND-01 pure-logic targets (`.env` parse/format/upsert, skill/prompt normalization) exist as standalone pure modules with delegating singletons and passing `node:test` suites, contributing to Success Criterion 2 (`make run_tests` covering the named pure-logic pieces).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-supervisor-tests-lint-makefile/01-01-SUMMARY.md` using the summary template.
</output>
