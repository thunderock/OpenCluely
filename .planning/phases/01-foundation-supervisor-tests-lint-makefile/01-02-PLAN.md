---
phase: 01-foundation-supervisor-tests-lint-makefile
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/vad-segmenter.js
  - src/services/speech.service.js
  - test/vad-segmenter.test.js
autonomous: true

must_haves:
  truths:
    - "vad-segmenter.js runs the VAD state machine as a pure module: energy/hysteresis/noise-floor adaptation/pre-roll ring/silence+speech accumulation, returning accumulate|flush|discard|noop actions"
    - "vad-segmenter takes tuning per ingest() call and imports no config/process.env/child_process (static rmsEnergy + chunkDurationMs are pure)"
    - "SpeechService delegates the VAD-enabled path of _ingestWhisperAudio, the VAD-enabled watchdog checks, _resetVadState and _endUtteranceFlush to the segmenter, with the VAD-disabled legacy path left byte-identical"
    - "node --test test/vad-segmenter.test.js passes, requiring ONLY src/core/vad-segmenter.js (never src/services/speech.service.js)"
  artifacts:
    - path: src/core/vad-segmenter.js
      provides: "Pure VAD segmentation state machine lifted verbatim from SpeechService"
      exports: ["VadSegmenter"]
      contains: "class VadSegmenter"
      min_lines: 60
    - path: test/vad-segmenter.test.js
      provides: "node:test coverage of onset/pre-roll, flush-on-pause, discard-noise, max-utterance flush, and the static energy/duration helpers"
      contains: "require('../src/core/vad-segmenter')"
  key_links:
    - from: src/services/speech.service.js
      to: src/core/vad-segmenter.js
      via: "this._segmenter.ingest(buffer, tuning) in _ingestWhisperAudio"
      pattern: "_segmenter\\.ingest\\("
    - from: src/services/speech.service.js
      to: src/core/vad-segmenter.js
      via: "require of the extracted segmenter"
      pattern: "require\\('\\.\\./core/vad-segmenter'\\)"
---

<objective>
Lift the voice-activity-detection state machine out of the 1847-line self-initializing `SpeechService` singleton into a pure `src/core/vad-segmenter.js`, have `SpeechService` delegate to it with ZERO behavior change, and pin the state machine with a deterministic `node:test` suite. This delivers the third and hardest FND-01 pure-logic target (VAD segmentation).

Purpose: `SpeechService` mutates `global.window`, `try/require`s the Azure SDK, and reads config/process.env at import time, so it cannot be required in a test. The VAD decision logic is a pure state machine buried inside it; extracting it makes segmentation testable without the app.
Output: `src/core/vad-segmenter.js`, delegating edits to `src/services/speech.service.js`, and a passing `test/vad-segmenter.test.js`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-supervisor-tests-lint-makefile/01-RESEARCH.md
@.planning/phases/01-foundation-supervisor-tests-lint-makefile/01-CONTEXT.md
@.planning/codebase/CONVENTIONS.md

# Extraction source — read the VAD block (700-907) and the getters (1200-1266) before editing.
# Verify these line numbers against the live file first.
@src/services/speech.service.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vad-segmenter.js and delegate SpeechService</name>
  <files>src/core/vad-segmenter.js, src/services/speech.service.js</files>
  <action>
Create `src/core/vad-segmenter.js` (src/core style: 2-space, single quotes). It owns the VAD-decision state ONLY; buffer storage and the Whisper spawn/flush stay in `SpeechService`. Contract:

```
class VadSegmenter {
  constructor() { this.reset(); }

  // Full reset — mirrors the VAD-owned half of _resetVadState (speech.service.js:716-725).
  reset() {
    this.speaking = false; this.speechMs = 0; this.silenceMs = 0;
    this.noiseFloor = 0; this.noiseInit = false;
    this.preRoll = []; this.preRollBufferedMs = 0;
  }

  // Mirrors the state-reset half of _endUtteranceFlush (speech.service.js:894-898).
  endUtterance() {
    this.speaking = false; this.speechMs = 0; this.silenceMs = 0;
    this.preRoll = []; this.preRollBufferedMs = 0;
  }

  static rmsEnergy(buffer) { /* VERBATIM from _chunkRmsEnergy, speech.service.js:781-792 */ }
  static chunkDurationMs(buffer) { return buffer.length / 32; } /* VERBATIM from :904-907 */

  // Run one chunk through the VAD-enabled decision (translate speech.service.js:815-889 EXACTLY).
  // tuning = { energyFloor, silenceHangoverMs, minUtteranceMs, maxUtteranceMs, preRollMs }
  // Returns { type: 'accumulate' | 'flush' | 'discard' | 'noop', buffers: Buffer[] }
  ingest(buffer, tuning) { ... }
}
module.exports = VadSegmenter;
module.exports.VadSegmenter = VadSegmenter;
```

Translate `ingest` from speech.service.js:815-889 with these mechanical substitutions and NO logic change:
- `this._chunkRmsEnergy(buffer)` -> `VadSegmenter.rmsEnergy(buffer)`; `this._chunkDurationMs(x)` -> `VadSegmenter.chunkDurationMs(x)`.
- `this._getVadEnergyFloor()` -> `tuning.energyFloor`; `_getSilenceHangoverMs` -> `tuning.silenceHangoverMs`; `_getMinUtteranceMs` -> `tuning.minUtteranceMs`; `_getMaxUtteranceMs` -> `tuning.maxUtteranceMs`; `_getPreRollMs` -> `tuning.preRollMs`.
- `this.vad*` fields -> `this.*` (speaking/speechMs/silenceMs/noiseFloor/noiseInit/preRoll; `vadPreRollMs` -> `preRollBufferedMs`).
- Instead of `this.segmentBuffers.push(...)` / `this._endUtteranceFlush()` / clearing `segmentBuffers`, RETURN an action:
  - ONSET (was 836-849, pre-roll prepended): set speaking=true, speechMs=0, silenceMs=0, speechMs+=chunkMs; snapshot `buffers = [...this.preRoll, buffer]`; clear preRoll/preRollBufferedMs; `return { type: 'accumulate', buffers }`.
  - BACKGROUND (was 850-860): adapt `noiseFloor = noiseFloor*0.95 + energy*0.05`; push buffer to preRoll; `preRollBufferedMs += chunkMs`; trim `while (preRollBufferedMs > tuning.preRollMs && preRoll.length > 1) { preRollBufferedMs -= chunkDurationMs(preRoll.shift()) }`; `return { type: 'noop', buffers: [] }`.
  - ALREADY SPEAKING (was 866-889): update `speechMs`/`silenceMs` FIRST (voiced -> speechMs+=chunkMs, silenceMs=0; else silenceMs+=chunkMs); compute pausedLongEnough/haveRealSpeech/tooLong; then:
    - flush `(pausedLongEnough && haveRealSpeech) || tooLong`: `return { type: 'flush', buffers: [buffer] }` WITHOUT resetting (the caller's _endUtteranceFlush calls endUtterance()).
    - discard `pausedLongEnough && !haveRealSpeech`: reset speaking=false, speechMs=0, silenceMs=0 inline (mirror :884-888; do NOT touch preRoll/noiseFloor); `return { type: 'discard', buffers: [] }`.
    - otherwise: `return { type: 'accumulate', buffers: [buffer] }`.

Faithfulness note on discard: the original pushed the chunk (866) then cleared the whole segment (884) — net empty. Returning `buffers: []` + having the service clear its segment produces the identical net state (empty segment, speaking reset).

Then edit `src/services/speech.service.js` (2-space, single quotes — match the file):
1. Add near the top requires: `const VadSegmenter = require('../core/vad-segmenter');`. In the constructor, add `this._segmenter = new VadSegmenter();` near the other field initializers.
2. `_resetVadState()` (716-725): keep `this.vadSpeaking=false; this.vadSpeechMs=0; this.vadLastChunkAt=0;` (still used by the legacy path + watchdog) and add `this._segmenter.reset();`. REMOVE the now-orphaned field assignments `vadSilenceMs/vadNoiseFloor/vadNoiseInit/vadPreRoll/vadPreRollMs` — grep-confirmed used only inside 716-907 (which this plan rewrites); silence/noise-floor/pre-roll state is now owned entirely by the segmenter, so `_endUtteranceFlush` no longer references `vadSilenceMs` either.
3. `_ingestWhisperAudio(buffer)` (800-890): leave the `if (!this._isVadEnabled()) { ... return; }` LEGACY branch (805-813) BYTE-IDENTICAL. Replace the VAD-enabled remainder (815-889) with:
   ```
   this.vadLastChunkAt = Date.now();
   const action = this._segmenter.ingest(buffer, {
     energyFloor: this._getVadEnergyFloor(),
     silenceHangoverMs: this._getSilenceHangoverMs(),
     minUtteranceMs: this._getMinUtteranceMs(),
     maxUtteranceMs: this._getMaxUtteranceMs(),
     preRollMs: this._getPreRollMs(),
   });
   for (const buf of action.buffers) { this.segmentBuffers.push(buf); this.segmentBytes += buf.length; }
   if (action.type === 'flush') this._endUtteranceFlush();
   else if (action.type === 'discard') { this.segmentBuffers = []; this.segmentBytes = 0; }
   ```
   (Building the tuning object from the getters each call preserves the original per-chunk re-read of settings.)
4. `_endUtteranceFlush()` (893-902): replace the `vadSilenceMs`/`vadPreRoll`/`vadPreRollMs` resets (speech.service.js ~lines 896-898) with `this._segmenter.endUtterance();`. Keep `this.vadSpeaking=false; this.vadSpeechMs=0;` (legacy fields). Keep the `this._flushWhisperSegment({ final: false }).catch(...)` call EXACTLY.
5. `_startSegmentWatchdog()` (733-759): leave the `if (!this._isVadEnabled())` legacy branch (744-748) UNCHANGED. In the VAD-enabled branch, swap `this.vadSpeaking` -> `this._segmenter.speaking` and `this.vadSpeechMs` -> `this._segmenter.speechMs` (lines 754-755).
6. REMOVE `_chunkRmsEnergy` (781-792) — grep-confirmed used only at 817, which is now gone. REPLACE the body of `_chunkDurationMs` (904-907) with `return VadSegmenter.chunkDurationMs(buffer);` (it is still called by the legacy branch at 810).

Before removing any field/method, re-grep to confirm no other reader: `grep -n "vadNoiseFloor\|vadNoiseInit\|vadPreRoll\|_chunkRmsEnergy" src/services/speech.service.js` should show matches ONLY inside the block you are rewriting.
  </action>
  <verify>`node --check src/core/vad-segmenter.js && node --check src/services/speech.service.js` exit 0; `grep -n "_chunkRmsEnergy" src/services/speech.service.js` returns nothing; `grep -n "_segmenter.ingest(" src/services/speech.service.js` matches; `grep -n "this.vadSpeaking\|this.vadSpeechMs" src/services/speech.service.js` shows them only in the legacy path, _resetVadState, _endUtteranceFlush (NOT the VAD-enabled watchdog branch).</verify>
  <done>vad-segmenter.js is a pure module (no require of config/child_process/electron); SpeechService constructs and delegates to it for the VAD-enabled ingest path, watchdog checks, reset, and end-of-utterance; the legacy VAD-disabled path is unchanged; both files pass `node --check`.</done>
</task>

<task type="auto">
  <name>Task 2: Deterministic node:test suite for VadSegmenter</name>
  <files>test/vad-segmenter.test.js</files>
  <action>
Create `test/vad-segmenter.test.js` (`node:test` + `node:assert/strict`), requiring ONLY `../src/core/vad-segmenter`. Add a local PCM helper (16kHz mono 16-bit => 16 samples/ms, 32 bytes/ms) so tests are deterministic:

```
function pcm(ms, amplitude = 0) {
  const samples = Math.round(ms * 16);
  const buf = Buffer.alloc(samples * 2);
  for (let i = 0; i < samples; i++) buf.writeInt16LE(amplitude, i * 2);
  return buf;
}
```
A constant-amplitude buffer has RMS energy `amplitude / 32768`. Use a tiny deterministic tuning, e.g. `{ energyFloor: 0.01, silenceHangoverMs: 100, minUtteranceMs: 100, maxUtteranceMs: 500, preRollMs: 40 }`. Use a loud amplitude (e.g. 3277 => energy ~0.1, well above the enter threshold) for voiced chunks and amplitude 0 for silence. Cover:

1. Static helpers: `VadSegmenter.chunkDurationMs(pcm(100)) === 100`; `VadSegmenter.rmsEnergy(pcm(50, 0)) === 0`; `rmsEnergy(pcm(20, 3277))` is within ~1e-3 of 0.1; `rmsEnergy(Buffer.alloc(0)) === 0`.
2. Onset + pre-roll: on a fresh segmenter, ingest 2-3 silence chunks (each returns `noop`, building the pre-roll), then ingest a loud chunk -> returns `type:'flush'|'accumulate'` with `type==='accumulate'`, `buffers.length >= 2` (pre-roll prepended + current), and `segmenter.speaking === true`.
3. Flush on pause after real speech: fresh segmenter; onset (loud); enough loud chunks that `speechMs >= minUtteranceMs`; then silence chunks until one ingest returns `{ type: 'flush' }` whose `buffers` includes the final chunk.
4. Discard short noise: fresh segmenter; one short loud chunk (`< minUtteranceMs`, e.g. `pcm(40, 3277)`) then silence chunks until an ingest returns `{ type: 'discard' }`; assert `segmenter.speaking === false` afterward.
5. Max-utterance flush without a pause: fresh segmenter; onset then keep feeding loud chunks (no silence) until an ingest returns `{ type: 'flush' }` (driven by `speechMs >= maxUtteranceMs`).
6. endUtterance() resets: after a flush action, call `segmenter.endUtterance()` and assert `speaking === false`, `speechMs === 0`, `silenceMs === 0`, `preRoll.length === 0`.

Keep each scenario in its own `test(...)` with a fresh `new VadSegmenter()` so ordering never matters.
  </action>
  <verify>`node --test test/vad-segmenter.test.js` passes with all cases green and exits 0 (no hang); `grep -n "speech.service" test/vad-segmenter.test.js` returns nothing (module-only requirement).</verify>
  <done>The VAD state machine is pinned by a deterministic suite proving onset/pre-roll, flush-on-pause, discard-noise, max-utterance flush, endUtterance reset, and the static helpers — all requiring only the extracted module.</done>
</task>

</tasks>

<verification>
- `node --test test/*.test.js` runs the VAD suite (plus any siblings) with zero failures and does NOT hang.
- `src/core/vad-segmenter.js` requires nothing from `../services`, `./config`, `child_process`, or `electron` (grep to confirm it is pure).
- Delegation is faithful: the VAD-disabled legacy branch of `_ingestWhisperAudio` and the legacy watchdog branch are byte-identical to the original; only the VAD-enabled paths now route through `this._segmenter`.
- Author all new/edited lines lint-clean for 01-04 (no-undef / no-unused-vars; 2-space single-quote style).
</verification>

<success_criteria>
The third FND-01 pure-logic target (VAD segmentation) exists as a standalone pure `VadSegmenter` with a delegating `SpeechService` and a passing deterministic `node:test` suite, completing the pure-logic coverage required by Success Criterion 2.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-supervisor-tests-lint-makefile/01-02-SUMMARY.md` using the summary template.
</output>
