---
phase: 01-foundation-supervisor-tests-lint-makefile
plan: 5
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified:
  - Makefile
  - .github/workflows/ci.yml
autonomous: true

must_haves:
  truths:
    - "Makefile defines exactly the four locked targets setup, setup-dev, run_tests, lint with tab-indented recipes"
    - "On a clean checkout, make setup, make setup-dev, make run_tests, and make lint all run and succeed (SC1)"
    - "make run_tests runs node --test test/*.test.js (portable on Node 20 + newer), executing the three pure-logic suites + the supervisor suite without running the fixture as a test"
    - "A new .github/workflows/ci.yml runs lint + tests on pull_request and push:main across an ubuntu+macOS matrix on Node 20, separate from the tag-only release.yml"
    - "The lint gate exits non-zero on a lint violation (SC2 acceptance shape), demonstrated locally"
  artifacts:
    - path: Makefile
      provides: "The four locked make targets wiring npm ci + node --test + npx eslint"
      contains: "run_tests"
      min_lines: 10
    - path: .github/workflows/ci.yml
      provides: "CI lint+test gate on PR + push:main, ubuntu+macOS, Node 20"
      contains: "pull_request"
      min_lines: 20
  key_links:
    - from: Makefile
      to: node --test
      via: "run_tests target invokes node --test test/*.test.js"
      pattern: "node --test test/\\*\\.test\\.js"
    - from: Makefile
      to: eslint
      via: "lint target invokes npx eslint ."
      pattern: "npx eslint \\."
    - from: .github/workflows/ci.yml
      to: eslint + node:test
      via: "npm ci --ignore-scripts then npx eslint . then node --test"
      pattern: "npm ci --ignore-scripts"
---

<objective>
Wire the four locked `make` targets (FND-03) and a new CI workflow (FND-02) that gates lint + tests on pull requests and pushes to `main` across an ubuntu+macOS matrix on Node 20. Verify Success Criterion 1 (all four targets succeed on a clean checkout) end-to-end, and demonstrate Success Criterion 2's acceptance shape (a lint violation fails the gate).

Purpose: This is the top of the safety-net dependency graph — it composes the tests (01-01/02/03) and the lint config (01-04) into the developer commands and the CI gate that protect every future refactor.
Output: a `Makefile` and `.github/workflows/ci.yml`, with SC1/SC2 verified.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-supervisor-tests-lint-makefile/01-RESEARCH.md
@.planning/phases/01-foundation-supervisor-tests-lint-makefile/01-CONTEXT.md
@.planning/codebase/STACK.md

# Mirror the conventions of the existing (tag-only) release workflow; the new ci.yml is separate.
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Makefile and verify all four targets (SC1)</name>
  <files>Makefile</files>
  <action>
Create `Makefile` at the repo root with EXACTLY the four locked targets (names fixed by convention — do not rename or add). Follow the RESEARCH "Makefile" example. CRITICAL: recipe lines MUST begin with a literal TAB, not spaces (GNU Make requirement) — a space-indented recipe fails with "missing separator".

- `.PHONY: setup setup-dev run_tests lint`
- `setup:` recipe `npm ci` (reproducible from the committed lockfile; installs runtime deps + Electron + the new eslint/globals devDeps).
- `setup-dev: setup` (dev/test/lint tooling comes from the same `npm ci`; kept as a distinct target per the fixed convention — prerequisite `setup` so it provisions on a clean checkout).
- `run_tests:` recipe `node --test test/*.test.js`. Use this portable invocation (shell expands the single `*`; runs only `*.test.js`, so `test/fixtures/dummy-service.js` is never executed as a test). Do NOT use `node --test test/` (would run the fixture and hang — Pitfall 1) and do NOT use a `**` globstar (Node 21+ only; CI is Node 20).
- `lint:` recipe `npx eslint .` (error-only gate — exits non-zero on any error).

Add a one-line comment noting that on a clean checkout `make setup` (or `make setup-dev`) must run before `make lint`/`make run_tests` (lint needs ESLint installed). Document this order; do NOT add node_modules sentinel prerequisites (keep it simple).

Then verify SC1 end-to-end from a clean-ish state: run `make setup` (or confirm deps already installed), then `make setup-dev`, `make run_tests`, and `make lint` — all four must exit 0. `run_tests` must execute the three pure-logic suites (env-file, skill-normalizer, vad-segmenter) plus the supervisor suite and report all passing without hanging.
  </action>
  <verify>`cat -A Makefile` shows recipe lines starting with `^I` (tab), not spaces; `make -n run_tests lint` prints the expected commands; `make setup && make setup-dev && make run_tests && make lint` all exit 0; the `run_tests` output lists passing tests from all four suites and terminates.</verify>
  <done>The Makefile has the four locked targets with tab-indented recipes, and all four run and succeed on the current checkout (Success Criterion 1).</done>
</task>

<task type="auto">
  <name>Task 2: Author ci.yml and demonstrate the lint gate (SC2)</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Create a NEW `.github/workflows/ci.yml` (separate from the tag-only `release.yml`, which stays unchanged) following the RESEARCH "CI workflow" example:
- `name: CI`
- `on: { pull_request: {}, push: { branches: [main] } }` — gates lint on ALL PRs (FND-02 SC2) and on pushes to main.
- One job `check` with `strategy: { fail-fast: false, matrix: { os: [ubuntu-latest, macos-latest] } }`, `runs-on: ${{ matrix.os }}`.
- Steps: `actions/checkout@v4`; `actions/setup-node@v4` with `node-version: '20'` (resolves to latest 20.x) and `cache: 'npm'`; `run: npm ci --ignore-scripts` (skip the Electron binary download + electron-builder app-deps rebuild — the lint/test job needs neither; faster and removes a flaky network dep — Pitfall 5); `run: npx eslint .` (the lint gate — a lint error fails the job); `run: node --test test/*.test.js`.

Validate the workflow file: confirm it is valid YAML and structurally correct (e.g. `node -e "require('fs').readFileSync('.github/workflows/ci.yml','utf8')"` plus a YAML parse if a parser is available, or `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"`). Confirm `release.yml` is untouched.

Demonstrate SC2's acceptance shape locally (Claude cannot push a real PR — per delivery policy we never push automatically; real CI verification happens when the user pushes). Prove the gate exits non-zero on a violation: write a throwaway file in a linted path with an intentional error, e.g. `printf 'module.exports = definitelyUndefinedGlobal;\n' > src/core/__lint_probe.js`, run `npx eslint src/core/__lint_probe.js; echo "exit=$?"` and confirm exit is non-zero (a `no-undef` error), then DELETE the probe (`rm src/core/__lint_probe.js`) and confirm `npx eslint .` is clean again. Since `ci.yml` runs the same `npx eslint .`, a PR carrying such a violation fails the CI lint gate.

Note in the SUMMARY that end-to-end CI-on-a-real-PR confirmation is pending the user's first push of a branch/PR (delivery policy: do not push automatically).
  </action>
  <verify>`.github/workflows/ci.yml` parses as valid YAML and contains `pull_request`, `push: branches: [main]`, the `ubuntu-latest`+`macos-latest` matrix, `node-version: '20'`, `npm ci --ignore-scripts`, `npx eslint .`, and `node --test test/*.test.js`; the lint-probe demonstration shows a non-zero exit on the intentional violation and the probe is removed; `npx eslint .` is clean afterward; `git status` shows `release.yml` unchanged.</verify>
  <done>ci.yml gates lint + tests on pull_request and push:main across ubuntu+macOS on Node 20 using `npm ci --ignore-scripts`; a lint violation is shown to exit the gate non-zero (SC2 acceptance shape); the existing release workflow is untouched.</done>
</task>

</tasks>

<verification>
- SC1: `make setup`, `make setup-dev`, `make run_tests`, `make lint` all succeed on the checkout.
- SC2: `make run_tests` executes the automated suite covering the three named pure-logic pieces (VAD segmentation, `.env` parse, skill/prompt normalization) plus the supervisor demo; and `npx eslint .` (the same command `ci.yml` runs) exits non-zero on an intentional lint violation, so a PR carrying one fails the CI lint gate.
- The Makefile recipes are tab-indented; `run_tests` uses `node --test test/*.test.js` (never a bare `test/` dir, which would run the fixture and hang).
- `ci.yml` is a new, separate workflow; `release.yml` is unchanged.
</verification>

<success_criteria>
FND-03 (the four-target Makefile) and the CI half of FND-02 are delivered and verified: all four make targets succeed on a clean checkout (SC1), the test suite covers the three pure-logic pieces (SC2 part 1), and the lint gate — wired into CI on PRs + push:main across ubuntu+macOS on Node 20 — fails on a lint violation (SC2 part 2).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-supervisor-tests-lint-makefile/01-05-SUMMARY.md` using the summary template.
</output>
