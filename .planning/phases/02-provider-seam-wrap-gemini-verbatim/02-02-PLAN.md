---
phase: 02-provider-seam-wrap-gemini-verbatim
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/services/providers/gemini.provider.js
  - src/services/providers/index.js
  - scripts/capture-gemini-goldens.js
  - test/fixtures/gemini-requests/text.json
  - test/fixtures/gemini-requests/image.json
  - test/fixtures/gemini-requests/transcription.json
  - test/gemini-request-parity.test.js
autonomous: true

must_haves:
  truths:
    - "The Gemini logic is relocated VERBATIM into a provider module implementing LLMProvider (generate/generateStream/isAvailable/testConnection) — byte-identical behavior, no rewrite (SC2)."
    - "The provider's serialize() maps the neutral struct to Gemini's contents/parts/systemInstruction/generationConfig wire format; this is the ONLY place Gemini wire shape is constructed (SC4)."
    - "A registry registers Gemini and exposes a hardcoded-default selected provider ('gemini') — no user-facing switch this phase."
    - "The refactored RequestBuilder+serialize pipeline reproduces a BYTE-IDENTICAL outgoing Gemini request vs today, for text/image/transcription, covering BOTH the generate and generateStream request-construction paths (parity golden test)."
    - "The app still runs on the existing llm.service (untouched this plan) — provider is additive; nothing removed yet."
  artifacts:
    - path: "src/services/providers/gemini.provider.js"
      provides: "GeminiProvider: verbatim transport/client/testConnection/network/error/fallback + serialize(neutral) + the 4 interface methods"
      contains: "class GeminiProvider extends LLMProvider"
    - path: "src/services/providers/index.js"
      provides: "Provider registry with hardcoded default selected provider 'gemini'"
      contains: "gemini"
    - path: "test/gemini-request-parity.test.js"
      provides: "Byte-identical golden assertion: serialize(requestBuilder.build*) === committed fixture, for all 3 shapes + stream==non-stream"
      contains: "JSON.stringify"
    - path: "test/fixtures/gemini-requests/text.json"
      provides: "Golden outgoing Gemini request captured from today's code (text)"
    - path: "test/fixtures/gemini-requests/image.json"
      provides: "Golden outgoing Gemini request captured from today's code (image)"
    - path: "test/fixtures/gemini-requests/transcription.json"
      provides: "Golden outgoing Gemini request captured from today's code (transcription)"
  key_links:
    - from: "src/services/providers/gemini.provider.js"
      to: "src/core/request-builder.js"
      via: "provider calls requestBuilder.build*() then this.serialize()"
      pattern: "requestBuilder|RequestBuilder|serialize"
    - from: "src/services/providers/gemini.provider.js"
      to: "src/services/providers/llm-provider.js"
      via: "extends LLMProvider, implements the 4 methods"
      pattern: "extends LLMProvider"
    - from: "test/gemini-request-parity.test.js"
      to: "test/fixtures/gemini-requests/*.json"
      via: "byte-identical JSON.stringify comparison against committed goldens"
      pattern: "gemini-requests"
---

<objective>
Relocate the Gemini logic VERBATIM into a dedicated provider that implements the `LLMProvider`
interface, add the neutral→wire `serialize()` step, register it behind a registry with a hardcoded
`gemini` default, and PIN parity with a byte-identical golden test. The existing
`src/services/llm.service.js` stays LIVE and untouched this plan (the facade flip is Plan 03), so the
app keeps working at every commit and the provider is proven before it is wired in.

Purpose: SC2 (provider implementing the interface, registered) + the parity guarantee that makes the
whole refactor safe. Output: the Gemini provider, the registry, and committed golden fixtures + parity test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-provider-seam-wrap-gemini-verbatim/02-CONTEXT.md
@.planning/phases/02-provider-seam-wrap-gemini-verbatim/02-01-SUMMARY.md

# The VERBATIM source to relocate + the neutral struct produced by Plan 01:
@src/services/llm.service.js            # ENTIRE file (1654 lines) — the god-file to relocate
@src/core/request-builder.js            # Plan-01 neutral struct producer (consumed by serialize)
@src/services/providers/llm-provider.js # Plan-01 interface to implement
@src/core/config.js                     # llm.gemini config block (43-56) + getApiKey — read from SAME place, no restructuring
@src/managers/session.manager.js        # singleton used by RequestBuilder (do not change)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GeminiProvider by relocating the god-file logic verbatim + add serialize() + registry</name>
  <files>src/services/providers/gemini.provider.js, src/services/providers/index.js</files>
  <action>
Create `class GeminiProvider extends LLMProvider` (require the Plan-01 interface). RELOCATE the Gemini
logic from `src/services/llm.service.js` as close to BYTE-IDENTICAL as feasible — only the mechanical
changes needed to fit the interface. NO logic rewrite, NO opportunistic refactor. Keep
`require('../../core/logger').createServiceLogger('LLM')` tag so log behavior is unchanged.

MOVE these members VERBATIM (copy the bodies exactly; adjust ONLY relative require paths — this file is
one level deeper than llm.service.js, so `../core/*` becomes `../../core/*`, `../managers/*` becomes
`../../managers/*`, `../../prompt-loader` becomes `../../../prompt-loader`):
- constructor state (client, model, isInitialized, requestCount, errorCount) + `initializeClient()`.
- `getGenerationConfig`, `applyGenerationDefaults`, `extractTextFromCandidates`.
- `processImageWithSkill`, `processImageWithSkillStream`, `processTextWithSkill`,
  `processTextWithSkillStream`, `processTranscriptionWithIntelligentResponse`,
  `processTranscriptionWithIntelligentResponseStream`.
- `enforceProgrammingLanguage`, `executeRequest`, `executeStreamingRequest`, `_extractChunkText`,
  `_streamRequestForModel`, `executeAlternativeRequest`, `_executeAlternativeRequestForModel`.
- `performPreflightCheck`, `getUserAgent`, `analyzeError`, `checkNetworkConnectivity`,
  `testNetworkConnection`, `generateFallbackResponse`, `generateIntelligentFallbackResponse`,
  `testConnection`, `_friendlyTestError`, `updateApiKey`, `getStats`, `delay`.
Config reads stay IDENTICAL (`config.get('llm.gemini.*')`, `config.getApiKey('GEMINI')`) — do NOT move or
restructure config (locked: Gemini config stays where it is).

REPLACE request construction with the RequestBuilder + serialize split (this is the ONLY surgery):
- Instantiate `this.requestBuilder = new (require('../../core/request-builder').RequestBuilder)()`.
- DELETE the moved copies of `buildGeminiRequest*`, `buildIntelligentTranscriptionRequest*`,
  `getIntelligentTranscriptionPrompt`, `formatUserMessage`, `formatImageInstruction` — they now live in
  RequestBuilder (Plan 01). In the process* methods, replace each `buildX(...)`/inline-image-assembly
  call with `this.serialize(this.requestBuilder.buildTextRequest(...))`,
  `...buildImageRequest(...)`, `...buildTranscriptionRequest(...)` respectively, passing the SAME args
  (text/image/skill/sessionMemory/programmingLanguage). Everything downstream (execute*, retries,
  metadata, fallback, logPerformance, enforceProgrammingLanguage) stays verbatim.

ADD `serialize(neutral)` — the neutral→Gemini wire mapper. It MUST reproduce today's exact object,
including KEY INSERTION ORDER so `JSON.stringify` is byte-identical:
```
serialize(neutral) {
  const request = { contents: [] };          // 1) contents key first
  this.applyGenerationDefaults(request);      // 2) sets request.generationConfig second
  if (neutral.systemPrompt) {                 // 3) systemInstruction third (only when present)
    request.systemInstruction = { parts: [{ text: neutral.systemPrompt }] };
  }
  for (const h of neutral.history) {
    request.contents.push({ role: h.role, parts: [{ text: h.content }] });
  }
  if (neutral.images && neutral.images.length) {
    request.contents.push({ role: 'user', parts: [
      { text: neutral.userText },
      ...neutral.images.map(i => ({ inlineData: { data: i.data, mimeType: i.mimeType } }))
    ]});
  } else {
    request.contents.push({ role: 'user', parts: [{ text: neutral.userText }] });
  }
  return request;
}
```
Verify against llm.service.js that this yields the SAME key order (contents, generationConfig,
systemInstruction) and the SAME parts shape for each of text (594-597 / 650-653), image (144-159), and
transcription (700-703 / 758-761). Do NOT include mdContext in the wire (unused this phase).

IMPLEMENT the 4 interface methods on the provider:
- `isAvailable()` → return `this.isInitialized` (mirror today's API-key/init readiness; SC decision: verbatim).
- `testConnection()` → already relocated verbatim.
- `generate(neutral, options = {})` → the serialize + non-streaming transport CORE: run the same
  primary/alternative + retry/fallback logic that the process* methods use (you may have the process*
  methods call this to avoid duplication, OR keep process* verbatim and add generate as a thin wrapper
  over `serialize`+`executeRequest`/`executeAlternativeRequest`). Return the text (after
  enforceProgrammingLanguage when a language is given). Prefer having process* funnel through
  generate/generateStream, but ONLY if it stays behavior-identical; if in doubt, keep process* verbatim
  and make generate/generateStream thin siblings — parity is judged by the golden test.
- `generateStream(neutral, options = {}, onDelta)` → serialize + `executeStreamingRequest`.

EXPORT: `module.exports = { GeminiProvider };` (export the CLASS; the registry instantiates it).

Create `src/services/providers/index.js` — the registry:
- Instantiate the concrete provider: `const gemini = new GeminiProvider();`
- A simple registry object: `{ providers: { gemini }, selected: 'gemini', register(name, p){...},
  get(name){ return this.providers[name]; }, getSelected(){ return this.providers[this.selected]; } }`.
- `selected` is the HARDCODED default 'gemini' — NO env/config switch this phase (locked; Phase 3 adds it).
- Export the registry (and optionally `getSelected`). This is where "when Gemini is selected" is expressed for SC3.
  </action>
  <verify>npx eslint src/services/providers/gemini.provider.js src/services/providers/index.js && node -e "const {GeminiProvider}=require('./src/services/providers/gemini.provider'); const p=new GeminiProvider(); console.log('impl:', ['generate','generateStream','isAvailable','testConnection'].every(m=>typeof p[m]==='function')); console.log('serialize keys:', JSON.stringify(Object.keys(p.serialize({skill:'dsa',systemPrompt:'SYS',userText:'U',images:[],history:[{role:'user',content:'h'}],mdContext:''}))))" && node -e "const r=require('./src/services/providers'); console.log('selected:', r.getSelected && r.getSelected().constructor.name)"</verify>
  <done>GeminiProvider extends LLMProvider, implements all 4 methods, exposes serialize() emitting keys in order [contents, generationConfig, systemInstruction]; registry returns the gemini instance as selected; eslint clean; llm.service.js still untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Capture golden fixtures from today's code + byte-identical parity test</name>
  <files>scripts/capture-gemini-goldens.js, test/fixtures/gemini-requests/text.json, test/fixtures/gemini-requests/image.json, test/fixtures/gemini-requests/transcription.json, test/gemini-request-parity.test.js</files>
  <action>
FIRST capture the golden baseline from TODAY's live `src/services/llm.service.js` (still present this
plan) so parity is provable against the real source of truth. Write `scripts/capture-gemini-goldens.js`
(network-free, run once):
- `require('../src/services/llm.service')` (the singleton) and `require('../src/managers/session.manager')`.
- Monkeypatch the singleton's transport to CAPTURE the outgoing request and short-circuit (network-free):
  set `llm.executeRequest`, `llm.executeAlternativeRequest`, and `llm.executeStreamingRequest` to
  functions that record their first argument (the `geminiRequest`) into a module var and resolve `''`.
- Monkeypatch sessionManager for determinism: `getConversationHistory = () => FIXED_HISTORY` (a fixed
  array incl. a system event + user + model + one empty event to exercise filtering) and
  `getSkillContext = () => ({ skillPrompt: 'SYSTEM_PROMPT_FIXED', requiresProgrammingLanguage: false })`.
  Also stub `llm.isInitialized = true` so the process* methods proceed.
- Call each process path with FIXED inputs (skill `'dsa'`, a fixed text, a tiny fixed image Buffer,
  programmingLanguage `null`): `processTextWithSkill`, `processImageWithSkill`,
  `processTranscriptionWithIntelligentResponse` (non-stream is enough to capture the request object —
  the streaming siblings build the SAME request object; assert that equivalence in the test).
- Write each captured `geminiRequest` to `test/fixtures/gemini-requests/{text,image,transcription}.json`
  via `JSON.stringify(request, null, 2)`. COMMIT these fixtures — they are the frozen baseline.

THEN write `test/gemini-request-parity.test.js` (node:test, top-level so `node --test test/*.test.js`
runs it) proving the refactored pipeline reproduces the goldens BYTE-FOR-BYTE:
- Build a RequestBuilder with the SAME fixed fakes used at capture (fixed history + skill context) and a
  fresh `GeminiProvider` (only its `serialize` + `applyGenerationDefaults` are exercised — network-free).
- For each shape, assert
  `JSON.stringify(provider.serialize(rb.buildXRequest(...fixedInputs...)), null, 2)` EQUALS the committed
  fixture string (read via fs). Use exact string equality (byte-identical), not deepEqual.
- Add an explicit "stream == non-stream request" assertion: the request object serialize produces is
  identical regardless of the generate vs generateStream path (both consume the same neutral+serialize),
  documenting that both interface paths are covered by these goldens; the streaming TRANSPORT is smoke-tested in Plan 03.
- Guard the generationConfig values come from `config.get('llm.gemini.generation')` unchanged (the fixture
  encodes them; equality covers it).
  </action>
  <verify>node scripts/capture-gemini-goldens.js && ls test/fixtures/gemini-requests/ && node --test test/gemini-request-parity.test.js && npx eslint scripts/capture-gemini-goldens.js test/gemini-request-parity.test.js</verify>
  <done>Three golden fixtures committed; parity test asserts byte-identical (string-equal) serialize output vs goldens for text/image/transcription; stream==non-stream request equivalence asserted; all pass; eslint clean.</done>
</task>

</tasks>

<verification>
- `node --test test/*.test.js` passes (Phase-1 suites + request-builder + gemini-request-parity).
- `npx eslint .` exits 0.
- `git diff --stat` shows `src/services/llm.service.js`, `main.js`, `src/managers/session.manager.js` UNCHANGED (provider is additive this plan).
- The provider implements exactly generate/generateStream/isAvailable/testConnection; registry returns gemini as the hardcoded selected default.
- serialize() emits `contents`, then `generationConfig`, then (conditionally) `systemInstruction` — proven byte-identical to today's output by the golden test.
</verification>

<success_criteria>
- Gemini is registered as a provider implementing the LLMProvider interface (SC2), with its logic relocated verbatim.
- serialize() is the single site of Gemini wire-format construction; no Gemini shape leaks into RequestBuilder (SC4).
- Byte-identical outgoing request proven for screenshot/typed-chat/voice shapes across both generate and generateStream request construction (parity guard).
- App unchanged/still working (llm.service live, nothing removed); lint + tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-provider-seam-wrap-gemini-verbatim/02-02-SUMMARY.md`
</output>
