---
phase: 02-provider-seam-wrap-gemini-verbatim
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/providers/llm-provider.js
  - src/core/request-builder.js
  - test/request-builder.test.js
autonomous: true

must_haves:
  truths:
    - "An LLMProvider contract naming exactly generate / generateStream / isAvailable / testConnection exists (SC2)."
    - "RequestBuilder produces ONE input-neutral request struct from (skill, text, image[], history, md-context) with NO Gemini wire shape (no contents/parts/systemInstruction/generationConfig) inside it (SC4, PROV-02)."
    - "RequestBuilder replicates today's exact history assembly + limits: 15 recent for text, 10-recent-then-last-8 for transcription; image path carries no history."
    - "RequestBuilder reuses the Phase-1 skill normalization (via prompt-loader/skill-normalizer) rather than re-implementing it."
    - "md-context is an accepted, defaulted-empty field on the neutral struct and is unused this phase."
  artifacts:
    - path: "src/services/providers/llm-provider.js"
      provides: "LLMProvider base/contract with generate/generateStream/isAvailable/testConnection"
      contains: "class LLMProvider"
    - path: "src/core/request-builder.js"
      provides: "Pure, DI-injectable RequestBuilder emitting the neutral request struct"
      contains: "class RequestBuilder"
    - path: "test/request-builder.test.js"
      provides: "node:test coverage of neutral output for text/image/transcription with injected history + skill context"
      contains: "node:test"
  key_links:
    - from: "src/core/request-builder.js"
      to: "src/core/skill-normalizer.js"
      via: "prompt-loader delegation (getSkillPrompt / getSkillContext)"
      pattern: "promptLoader|getSkillPrompt|getSkillContext"
    - from: "test/request-builder.test.js"
      to: "src/core/request-builder.js"
      via: "constructor DI of fake sessionManager + promptLoader"
      pattern: "new RequestBuilder\\("
---

<objective>
Land the pure, network-free abstraction skeleton FIRST (sequencing keystone): the `LLMProvider`
interface contract and the `RequestBuilder` that turns the app's known inputs into ONE
input-neutral request struct. No Gemini code moves yet, no call-site changes, nothing removed.

This plan carves the prompt/skill/system/history ASSEMBLY out of today's Gemini god-file
(`src/services/llm.service.js`) into a reusable pure module, exactly per the locked decision
"RequestBuilder owns prompt assembly; no prompt logic inside any provider."

Purpose: PROV-02 / SC4 foundation, and the testable seam every later plan depends on.
Output: `src/services/providers/llm-provider.js`, `src/core/request-builder.js`, `test/request-builder.test.js`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-provider-seam-wrap-gemini-verbatim/02-CONTEXT.md

# Source of truth for the VERBATIM assembly logic to relocate (READ these regions):
@src/services/llm.service.js        # buildGeminiRequest(556-600), buildGeminiRequestWithHistory(602-665), buildIntelligentTranscriptionRequest(667-713), buildIntelligentTranscriptionRequestWithHistory(715-779), getIntelligentTranscriptionPrompt(781-835), formatUserMessage(837-839), formatImageInstruction(300-303), image assembly inline in processImageWithSkill(141-159)
@src/managers/session.manager.js    # getConversationHistory(194-207), getSkillContext(231-260) — singleton
@prompt-loader.js                   # promptLoader singleton: getSkillPrompt, getRequestComponents, requiresProgrammingLanguage
@src/core/skill-normalizer.js       # Phase-1 pure module RequestBuilder must reuse (via prompt-loader), not re-implement
@src/core/service-supervisor.js     # Phase-1 DI-for-testability pattern to imitate (export class, inject collaborators)
@test/skill-normalizer.test.js      # node:test conventions to match
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the LLMProvider interface contract</name>
  <files>src/services/providers/llm-provider.js</files>
  <action>
Create the provider-agnostic contract (SC2). CommonJS + vanilla JS, no TS/framework.

- Export a `class LLMProvider` documenting the FOUR interface methods EXACTLY: `generate`,
  `generateStream`, `isAvailable`, `testConnection`. Each default implementation throws
  `new Error('LLMProvider.<method> not implemented')` so a subclass must override it.
- Add a one-line JSDoc per method describing intent only (keep comments succinct per repo rules):
  - `generate(neutralRequest, options)` → non-streaming answer.
  - `generateStream(neutralRequest, options, onDelta)` → streaming answer; onDelta(deltaText).
  - `isAvailable()` → boolean, provider ready to serve (mirrors today's Gemini readiness check).
  - `testConnection()` → `{ success, ... }` connectivity probe.
- `module.exports = { LLMProvider };`
- Do NOT add config, registry, or Gemini logic here — contract only. Do NOT add a 5th interface method.
- Use logger only if needed: `require('../core/logger').createServiceLogger('PROVIDER')`; never interpolate variables into the message string.
  </action>
  <verify>node -e "const {LLMProvider}=require('./src/services/providers/llm-provider'); const p=new LLMProvider(); ['generate','generateStream','isAvailable','testConnection'].forEach(m=>{try{p[m]()}catch(e){console.log(m,'throws OK')}})" && npx eslint src/services/providers/llm-provider.js</verify>
  <done>The four methods exist and throw-by-default; eslint clean; no Gemini/registry code present.</done>
</task>

<task type="auto">
  <name>Task 2: Build the pure, DI-injectable RequestBuilder (neutral struct)</name>
  <files>src/core/request-builder.js</files>
  <action>
Create `class RequestBuilder` as a pure module in the Phase-1 `src/core/` spirit (imitate
`service-supervisor.js` DI: export the class, inject collaborators, default to the real singletons).

Constructor: `constructor({ sessionManager, promptLoader } = {})` →
`this.sessionManager = sessionManager || require('../managers/session.manager')` and
`this.promptLoader = promptLoader || require('../../prompt-loader').promptLoader`.

Define the NEUTRAL request struct (the ONLY output shape — provider-agnostic, NO Gemini wire keys):
```
{
  kind: 'text' | 'image' | 'transcription',
  skill: string,
  systemPrompt: string | null,   // fully-assembled system-instruction TEXT (or null)
  userText: string,              // the assembled user message text
  images: [{ data: base64String, mimeType: string }],  // [] when no image
  history: [{ role: 'user' | 'model', content: string }],  // assembled + filtered + truncated + role-mapped
  mdContext: string              // accepted param, DEFAULT '' , UNUSED this phase (Phase 5 wires a source)
}
```

Move these helpers VERBATIM from `src/services/llm.service.js` (byte-identical strings — they are
prompt content; do NOT reword):
- `formatUserMessage(text, activeSkill)` (llm.service.js:837-839).
- `formatImageInstruction(activeSkill, programmingLanguage)` (llm.service.js:300-303).
- `getIntelligentTranscriptionPrompt(activeSkill, programmingLanguage)` (llm.service.js:781-835) — copy the entire template string EXACTLY.

Implement three build methods that CARVE OUT the assembly logic from today's build* methods
(take everything that computes strings/history; LEAVE the contents/parts/systemInstruction/generationConfig
wire-mapping for the provider's serialize step in Plan 02 — that mapping does NOT belong here):

- `buildTextRequest(text, activeSkill, sessionMemory = [], programmingLanguage = null, mdContext = '')`
  Replicate `buildGeminiRequest` (llm.service.js:556-600) branching EXACTLY:
  * If `this.sessionManager.getConversationHistory` is a function: use
    `getConversationHistory(15)` + `getSkillContext(activeSkill, programmingLanguage)`; set
    `systemPrompt = skillContext.skillPrompt || null`; assemble `history` from the 15 events with the
    SAME filter (`role !== 'system' && content is non-empty string`) and role map
    (`role === 'model' ? 'model' : 'user'`) as buildGeminiRequestWithHistory (602-653);
    `userText = formatUserMessage(text, activeSkill)`.
  * Else (fallback branch): replicate via `promptLoader.getRequestComponents(...)` → systemPrompt from
    `shouldUseModelMemory && skillPrompt`, empty history, `userText = formatUserMessage(text, activeSkill)`.
  * `kind:'text'`, `images: []`.

- `buildImageRequest(imageBufferOrBase64, mimeType, activeSkill, programmingLanguage = null, mdContext = '')`
  Replicate the inline image assembly (processImageWithSkill 141-159):
  * `systemPrompt = promptLoader.getSkillPrompt(activeSkill, programmingLanguage) || ''` → emit as
    `systemPrompt` ONLY when non-empty-after-trim (else null), matching today's `if (skillPrompt.trim().length>0)`.
  * `userText = formatImageInstruction(activeSkill, programmingLanguage)`.
  * `images: [{ data: <base64>, mimeType }]` (accept a Buffer and `.toString('base64')`, matching today).
  * `history: []`, `kind:'image'`.

- `buildTranscriptionRequest(text, activeSkill, sessionMemory = [], programmingLanguage = null, mdContext = '')`
  Replicate `buildIntelligentTranscriptionRequest` (667-779) EXACTLY:
  * cleanText = `text && typeof text === 'string' ? text.trim() : ''`; throw the SAME error if empty.
  * If `getConversationHistory` is a function: `getConversationHistory(10)` then `.slice(-8)`, SAME filter
    + role map + null-drop as 715-747; `systemPrompt = getIntelligentTranscriptionPrompt(activeSkill, programmingLanguage)`
    (ALWAYS set for transcription); `userText = cleanText`.
  * Else fallback branch (683-712): empty history, same systemPrompt, `userText = cleanText`.
  * `kind:'transcription'`, `images: []`.

Do NOT compute generationConfig here (that is Gemini wire config → provider). Do NOT build
`contents`/`parts`/`systemInstruction` objects here. Preserve the exact history LIMITS (15; 10 then slice(-8)).
Logger: `require('./logger').createServiceLogger('REQUEST-BUILDER')` if you log; never interpolate variables into the message.
`module.exports = { RequestBuilder };`
  </action>
  <verify>node -e "const {RequestBuilder}=require('./src/core/request-builder'); const rb=new RequestBuilder({sessionManager:{}, promptLoader:{getSkillPrompt:()=>'P', getRequestComponents:()=>({shouldUseModelMemory:true,skillPrompt:'P'}), requiresProgrammingLanguage:()=>false}}); const n=rb.buildTextRequest('hi','dsa',[], null); console.log(JSON.stringify(Object.keys(n))); console.log('has contents?', 'contents' in n)" && npx eslint src/core/request-builder.js</verify>
  <done>Three build methods return the neutral struct; NO Gemini wire keys (contents/parts/systemInstruction/generationConfig) appear in output; exact limits (15; 10→slice(-8)) preserved; prompt strings byte-identical to llm.service.js; eslint clean.</done>
</task>

<task type="auto">
  <name>Task 3: node:test suite for RequestBuilder neutral output</name>
  <files>test/request-builder.test.js</files>
  <action>
Write a `node:test` suite (match `test/skill-normalizer.test.js` conventions: `require('node:test')`,
`require('node:assert/strict')`, top-level file so `node --test test/*.test.js` runs it).

Construct RequestBuilder with FAKE injected collaborators (DI) so it is fully network-free and
deterministic:
- fake `sessionManager` with `getConversationHistory(n)` returning a FIXED array of events (mix of
  system + user + model + one empty-content event to prove filtering) and `getSkillContext(skill, lang)`
  returning `{ skillPrompt: 'SYS', requiresProgrammingLanguage: false }`.
- fake `promptLoader` with `getSkillPrompt`, `getRequestComponents`, `requiresProgrammingLanguage`.

Assert:
- `buildTextRequest`: neutral has `systemPrompt === 'SYS'`; `userText` equals
  `Context: DSA analysis request\n\nText to analyze:\n<text>`; `history` excludes system + empty events,
  maps model→model / user→user, and honors the 15 cap (feed >15 to prove slice).
- `buildImageRequest`: `images[0].mimeType` set, `images[0].data` is the base64 of the input buffer,
  `userText` starts with `Analyze this image for a DSA question`, `history` empty.
- `buildTranscriptionRequest`: `systemPrompt` contains `Intelligent Transcription Response System`;
  history honors 10-then-last-8 (feed >10 to prove `.slice(-8)`); empty/whitespace text throws.
- The neutral struct NEVER contains `contents`, `parts`, `systemInstruction`, or `generationConfig`
  (assert those keys are absent) — this pins the "no wire shape in the builder" contract.
- `mdContext` defaults to `''` and is passed through.
  </action>
  <verify>node --test test/request-builder.test.js</verify>
  <done>All RequestBuilder tests pass; suite proves neutral output shape, exact limits, filtering, and absence of Gemini wire keys.</done>
</task>

</tasks>

<verification>
- `npx eslint .` exits 0 (whole-repo gate, matching CI).
- `node --test test/*.test.js` passes (existing Phase-1 suites + new request-builder suite).
- `src/services/llm.service.js`, `main.js`, `src/managers/session.manager.js` are UNCHANGED (this plan is purely additive).
- RequestBuilder emits neutral structs only; no `contents`/`parts`/`systemInstruction`/`generationConfig` anywhere in `src/core/request-builder.js`.
</verification>

<success_criteria>
- LLMProvider contract exists with exactly generate/generateStream/isAvailable/testConnection (SC2 groundwork).
- RequestBuilder produces one neutral request shape from (skill, text/image, history, md-context) with no prompt logic destined for providers (SC4 / PROV-02 groundwork).
- Today's exact history assembly/limits and prompt strings are preserved byte-for-byte, proven by tests.
- Zero changes to call-sites or the live Gemini service; lint + tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-provider-seam-wrap-gemini-verbatim/02-01-SUMMARY.md`
</output>
