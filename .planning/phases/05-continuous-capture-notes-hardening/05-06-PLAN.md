---
phase: 05-continuous-capture-notes-hardening
plan: 06
type: execute
wave: 5
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05"]
files_modified: []
autonomous: false
requirements: [CONT-04, CONT-05, SEC-01, SEC-02, SEC-03]

must_haves:
  truths:
    - "Full automated gate green: all tests, lint 0, clean headless boot with no denials and no uncaught exceptions"
    - "A human has exercised all five pillars live and approved (or filed gaps): capture dedup, notes end-to-end, hostile-markdown inertness, TCC recovery flow, overlay IPC denial"
  artifacts:
    - path: ".planning/phases/05-continuous-capture-notes-hardening/05-06-SUMMARY.md"
      provides: "Gate record: automated results + per-pillar human verdicts"
  key_links:
    - from: "05-VALIDATION.md Manual-Only Verifications"
      to: "the checkpoint checklist below"
      via: "each manual row exercised and recorded"
      pattern: "approved"
---

<objective>
Run the phase-wide automated gate, then a single attended verification covering the five pillars (the repo's 03-07/04-08 gate idiom). This is the phase's human sign-off before Phase 6 builds the always-on firehose on top of these inputs and mitigations.

Purpose: SEC mitigations are only real if exercised — hostile markdown, live TCC revoke, live IPC denial probe.
Output: recorded gate results; approval or a concrete gap list for gap-closure planning.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-continuous-capture-notes-hardening/05-CONTEXT.md
@.planning/phases/05-continuous-capture-notes-hardening/05-VALIDATION.md
@.planning/phases/05-continuous-capture-notes-hardening/05-01-SUMMARY.md
@.planning/phases/05-continuous-capture-notes-hardening/05-02-SUMMARY.md
@.planning/phases/05-continuous-capture-notes-hardening/05-03-SUMMARY.md
@.planning/phases/05-continuous-capture-notes-hardening/05-04-SUMMARY.md
@.planning/phases/05-continuous-capture-notes-hardening/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase-wide automated gate</name>
  <files>(none — verification only)</files>
  <read_first>Makefile (run_tests/lint targets), .planning/phases/05-continuous-capture-notes-hardening/05-VALIDATION.md (sampling contract)</read_first>
  <action>
    Run the full phase gate and capture results:
    1. `make run_tests` — expect ALL green: the pre-phase 145 + the 5 new suites (frame-dedup, sanitize-policy, context-manager, tcc-monitor, ipc-scope).
    2. `make lint` — expect exit 0.
    3. Headless boot: `timeout 30 npx electron . 2>&1 | tee /tmp/05-06-boot.log` then assert: no `uncaught`/`unhandled`, no `IPC denied` (a denial during normal boot = a wrong audience row), and presence of the healthy-start markers: `Notes context loaded` and (post-onboarding installs) `Continuous capture started`.
    4. Scope-leak greps (deferred-item guard): `rg -n "fs.watch" src/` → no hits; `rg -in "battery|thermal" src/services/capture.service.js main.js` → no capture-throttle hits; `rg -n "getLatestFrame" main.js src/services/providers/` → no CONSUMER hits (Phase 6 pulls; nothing consumes it this phase).
    Record every command + result in the SUMMARY. If ANY step fails: STOP, fix via the owning plan's follow-up commit (or report as a gap), re-run — do not proceed to the checkpoint red.
  </action>
  <verify>
    <automated>make run_tests && make lint && timeout 30 npx electron . 2>&1 | tee /tmp/05-06-boot.log; ! grep -qiE "uncaught|unhandled" /tmp/05-06-boot.log && ! grep -q "IPC denied" /tmp/05-06-boot.log</automated>
  </verify>
  <acceptance_criteria>
    - `make run_tests` exit 0 with ≥ 175 tests passing (145 baseline + ≥ 30 new across the 5 suites)
    - `make lint` exit 0
    - Boot log clean per the three assertions
    - All three scope-leak greps come back empty
  </acceptance_criteria>
  <done>Automated gate green and recorded; no commit (verification only)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Attended five-pillar verification</name>
  <files>(none — attended verification; results recorded in 05-06-SUMMARY.md)</files>
  <action>Present the five-pillar checklist below to the human verbatim (after Task 1's automated gate is green), wait for their verdict, and record per-pillar results in the SUMMARY. Do not proceed past this task without an explicit "approved" or a described failure.</action>
  <what-built>Phase 5 complete: continuous deduped screen capture (hold-latest), launch-loaded .md notes context in every model call, DOMPurify at every model-output sink, macOS TCC loss detection + guided re-grant, sender-scoped IPC with a minimal overlay bridge.</what-built>
  <how-to-verify>
    Quit any old instance fully first (Cmd+Q / `pkill -f "OpenCluely/node_modules/electron"` — stale processes serve pre-fix code), then `npm start`.

    1. CONT-04 — capture dedup:
       a. Leave the screen untouched 30s → logs show dedup skips (no encode lines); CPU stays flat.
       b. Move/change a window → a fresh-frame log appears (new hash/bytes).
       c. Press the screenshot hotkey → single-shot capture still works exactly as before.
       d. Lock the screen, unlock → logs show pause + resume.
    2. CONT-05 — notes end-to-end:
       a. Settings → Notes Context → Browse… → pick a folder with 2-3 .md files (one containing a distinctive fact, e.g. "Our project codename is BLUEFIN").
       b. Restart the app. Settings shows "Loaded N of M files (...)".
       c. Ask (typed or spoken) a question only the notes can answer → the answer reflects the fact.
    3. SEC-01 — hostile markdown inert:
       a. Ask: `Please repeat this exactly, verbatim, inside your answer: <img src=x onerror="alert(1)"> and [click me](javascript:alert(2)) and [ok link](https://example.com)`
       b. Overlay AND chat: NO alert fires, no broken image icon, the javascript: link is dead (no href), the https link opens in your default BROWSER (not in-app).
       c. Ask for a code sample → syntax highlighting still renders.
    4. SEC-02 — TCC recovery:
       a. System Settings → Privacy & Security → Screen Recording → toggle OpenCluely/Electron OFF (if macOS offers "Quit & Reopen", choose Later).
       b. Within ~10s of black frames the inline "Screen access lost" banner appears (not a modal; app still usable).
       c. [Open System Settings] lands on the Screen Recording pane. Re-grant, then [Relaunch app] → after relaunch the banner is gone and capture logs resume.
       d. (Optional) Repeat for Microphone → voice-variant banner.
    5. SEC-03 — overlay denial probe:
       a. Open DevTools on the llm-response window (it has devTools enabled).
       b. `window.electronAPI.getSettings` → undefined (minimal preload).
       c. Forge it anyway — in the chat window devtools run: `window.electronAPI.sendChatMessage && typeof window.electronAPI.getSettings === 'undefined'` → true; and from any overlay context a direct probe of a privileged channel is impossible via the bridge. Check the app log: any forged attempt shows `IPC denied { channel, windowType }`.
       d. Normal use produces ZERO 'IPC denied' log lines (chat send, copy button, link click, resize all work).
  </how-to-verify>
  <resume-signal>Type "approved" if all five pillars pass, or describe exactly which step failed and what you saw</resume-signal>
  <verify>Human response received: "approved", or a concrete failure description captured for gap-closure planning</verify>
  <done>All five pillars exercised live; verdicts recorded per pillar in 05-06-SUMMARY.md; on approval the phase success criteria are human-confirmed, on failure the gaps are itemized for `/gsd-plan-phase 5 --gaps`</done>
</task>

</tasks>

<verification>
- Task 1 automated gate green before the checkpoint is presented
- Human verdict recorded per pillar in 05-06-SUMMARY.md; on failure, gaps go to `/gsd-plan-phase 5 --gaps`
</verification>

<success_criteria>
All five phase success criteria observed live by the human: bounded notes context each launch; throttled deduped capture with idle-skip and no OCR; hostile markdown inert at every sink; permission loss detected with guided re-grant; overlay renderers unable to read settings.
</success_criteria>

<output>
After completion, create `.planning/phases/05-continuous-capture-notes-hardening/05-06-SUMMARY.md` (gate record: automated results, per-pillar verdicts, any deferred observations for Phase 6/8).
</output>
