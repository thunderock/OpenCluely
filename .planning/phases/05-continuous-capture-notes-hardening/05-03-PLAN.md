---
phase: 05-continuous-capture-notes-hardening
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [src/core/context.manager.js, test/context-manager.test.js, src/core/config.js, src/services/providers/local.provider.js, main.js, preload.js, settings.html, src/ui/settings-window.js]
autonomous: true
requirements: [CONT-05]

must_haves:
  truths:
    - "On startup the app loads a settings-configured folder of .md files as a bounded string (12,000 chars default, configurable), reloaded each launch"
    - "Every model call (text, image, transcription) carries the loaded notes in the system prefix via RequestBuilder.mdContext"
    - "Over budget: whole files in stable alphabetical order, stop before the file that would bust the budget; 'N of M files loaded' surfaced in settings"
    - "Settings offers a native folder picker AND an editable text path; change applies on restart (launch-only reload)"
  artifacts:
    - path: "src/core/context.manager.js"
      provides: "Notes loader: load()/getContext()/getStatus() + pure selectFilesWithinBudget"
      exports: ["ContextManager", "selectFilesWithinBudget", "contextManager"]
    - path: "test/context-manager.test.js"
      provides: "node:test coverage of budget selection + loader (tmpdir)"
    - path: "src/core/config.js"
      provides: "config.notes block (folder, budgetChars)"
      contains: "notes:"
  key_links:
    - from: "src/services/providers/local.provider.js"
      to: "src/core/context.manager.js"
      via: "contextManager.getContext() passed as the mdContext arg at all 3 build*Request call sites"
      pattern: "getContext\\(\\)"
    - from: "main.js onAppReady"
      to: "contextManager.load()"
      via: "isolated try/catch startup load"
      pattern: "contextManager"
    - from: "src/ui/settings-window.js"
      to: "select-notes-folder IPC"
      via: "Browse button → dialog.showOpenDialog(directory) → path into the editable field"
      pattern: "selectNotesFolder"
---

<objective>
Build the CONT-05 notes loader: read a settings-configured folder of `.md` files at launch into ONE bounded string (12k chars default) and wire it into the existing `RequestBuilder.mdContext` seam so every model call carries the user's notes in the system prefix. Settings gains a native folder picker + editable path + "N of M files loaded" status.

Purpose: the user's own notes become standing context for every answer — the third input of the core value ("screen + speech + your own notes").
Output: `src/core/context.manager.js` (+tests), `config.notes`, 3-line provider wiring, `select-notes-folder` IPC, settings UI row.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-continuous-capture-notes-hardening/05-CONTEXT.md
@.planning/phases/05-continuous-capture-notes-hardening/05-RESEARCH.md
@.planning/phases/05-continuous-capture-notes-hardening/05-01-SUMMARY.md
@src/services/providers/local.provider.js
@src/core/config.js

<interfaces>
The pre-wired seam (Phase 2/3 — DO NOT touch serialize/RequestBuilder internals):

From src/core/request-builder.js (signatures — mdContext is the LAST param):
```js
buildTextRequest(text, activeSkill, sessionMemory = [], programmingLanguage = null, mdContext = '')
buildImageRequest(imageBufferOrBase64, mimeType, activeSkill, programmingLanguage = null, mdContext = '')
buildTranscriptionRequest(text, activeSkill, _sessionMemory = [], programmingLanguage = null, mdContext = '')
```

From src/services/providers/local.provider.js — THE ONLY THREE CALL SITES (verified: main.js never calls RequestBuilder directly):
```js
// :259  const neutral = this.requestBuilder.buildImageRequest(imageBuffer, mimeType, activeSkill, programmingLanguage);
// :287  const neutral = this.requestBuilder.buildTextRequest(text, activeSkill, sessionMemory, programmingLanguage);
// :313  const neutral = this.requestBuilder.buildTranscriptionRequest(text, activeSkill, sessionMemory, programmingLanguage);
```
serialize() (local.provider.js:95) already does `[neutral.systemPrompt, neutral.mdContext].filter(Boolean).join('\n\n')` — position-stable system prefix, `/no_think` appended after. NO provider changes beyond the 3 call-site args + one require.

Settings persistence pattern (main.js:1918-2060): getSettings() returns a flat object; saveSettings(settings) collects `envUpdates.KEY = value` → `persistEnvUpdates` → `.env`; config.js reads `process.env.X` at load ⇒ restart-to-apply. Mirror it for NOTES_FOLDER.

Budget pre-validated: 03-07 smoke measured TTFT with ~12k chars of filler mdContext through the real prefill path — cite in SUMMARY, do NOT re-run.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: context.manager.js loader + pure budget selection + config.notes</name>
  <files>src/core/context.manager.js, test/context-manager.test.js, src/core/config.js</files>
  <read_first>src/core/env-file.js + test/env-file.test.js (pure-module + node:test shape), src/core/config.js (block placement — notes goes after llm), src/core/logger.js (createServiceLogger usage)</read_first>
  <behavior>
    Test cases (tmpdir-based, no mocks needed):
    - selectFilesWithinBudget (PURE — operates on [{name, chars}] pre-sorted, budget): files [a:5000, b:5000, c:5000], budget 12000 → loaded [a,b], total 10000 (stop-before-bust: c would exceed)
    - selectFilesWithinBudget: first file alone exceeds budget → loaded [], total 0 (stop at FIRST bust — whole files, stable order, NO skip-and-continue)
    - selectFilesWithinBudget: empty list → { loaded: [], total: 0 }
    - loader integration (os.tmpdir() fixture dir): writes b.md/a.md/.hidden.md/notes.txt + a subdir with d.md → load() picks ONLY top-level non-dot .md files, alphabetical (a.md before b.md), context contains `# a.md` header before `# b.md` header, `.hidden.md`/`notes.txt`/subdir ignored; getStatus() → { loadedCount: 2, totalCount: 2, ... }
    - loader with folder unset ('') → getContext() === '', getStatus().loadedCount === 0, no throw
    - loader with nonexistent folder path → getContext() === '', warn-logged, no throw (degrade-never-crash)
    - budget counts the ASSEMBLED string: with budget 20 and files whose headers+separators+content exceed 20 → assembly stops so that getContext().length <= 20
  </behavior>
  <action>
    1. `src/core/config.js` — add after the `llm` block:
    ```js
    notes: {
      folder: process.env.NOTES_FOLDER || '',
      budgetChars: parseInt(process.env.NOTES_BUDGET_CHARS, 10) || 12000
    },
    ```
    2. Create `src/core/context.manager.js`:
    - `function selectFilesWithinBudget(files, budget)` — PURE: files = [{name, chars}] already sorted; iterate, accumulate, STOP (break) at the first file where total + chars > budget; return { loaded: [names], total }.
    - `class ContextManager` — constructor(options = {}) taking `{ folder, budgetChars }` overrides (DI for tests; defaults from `require('./config').get('notes.folder'|'notes.budgetChars')` read at load() time, not construct time — launch-only semantics).
      - `async load()`: if no folder → reset to empty state, info-log, return status. `fs.promises.readdir(folder, { withFileTypes: true })`; keep `d.isFile() && d.name.endsWith('.md') && !d.name.startsWith('.')` (TOP-LEVEL only, no recursion); sort names with default codepoint sort (`.sort()` — locale-independent); read each utf8. Assemble incrementally: for each file, candidate block = (first ? '' : '\n\n---\n\n') + `# ${name}\n\n${content}`; STOP before appending a block that would push the assembled string past budgetChars (budget applies to the FINAL assembled string including headers/separators — the string that hits the prompt). Track loadedCount/totalCount/chars. Wrap the whole body in try/catch → warn-log `{ folder, error }`, empty context, NEVER throw.
      - `getContext()` → the cached assembled string ('' before load / on failure).
      - `getStatus()` → `{ folder, loadedCount, totalCount, chars, budget }`.
    - Logging: `createServiceLogger('CONTEXT')`; messages constant, variables in metadata.
    - Export: `module.exports = { ContextManager, selectFilesWithinBudget, contextManager: new ContextManager() };` (singleton constructed lazily-safe: constructor does NO fs and NO config reads).
    3. Tests per behavior block — RED first for the pure function, then implement (GREEN). Integration cases build fixture dirs under `fs.mkdtempSync(path.join(os.tmpdir(), 'notes-'))` and clean up.
  </action>
  <verify>
    <automated>node --test test/context-manager.test.js && npx eslint src/core/context.manager.js test/context-manager.test.js src/core/config.js && make run_tests</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/context.manager.js` exports `ContextManager`, `selectFilesWithinBudget`, `contextManager`
    - `config.js` contains `NOTES_FOLDER` and `budgetChars` with 12000 default under `notes:`
    - `node --test test/context-manager.test.js` exits 0 with ≥ 7 passing tests
    - Loader contains `withFileTypes` and `.endsWith('.md')` and `startsWith('.')` (top-level, md-only, no dotfiles)
    - No `fs.watch` anywhere in the module (launch-only — locked)
  </acceptance_criteria>
  <done>Bounded loader proven under node:test; commit `feat(05-03): notes context manager with whole-file budget` with pathspec `git commit -- src/core/context.manager.js test/context-manager.test.js src/core/config.js`</done>
</task>

<task type="auto">
  <name>Task 2: Wire mdContext into the provider + startup load + settings IPC/persistence</name>
  <files>src/services/providers/local.provider.js, main.js, preload.js</files>
  <read_first>src/services/providers/local.provider.js lines 250-335 (the three process* methods — exact current call shapes), main.js onAppReady (where getLocalModelManager().start() runs — the isolated-try/catch idiom), main.js:799 get-settings + :1031 save-settings + :1918-2060 (getSettings/saveSettings/persistEnvUpdates), main.js:846 open-external (handler SHAPE for the new select-notes-folder handler), preload.js (electronAPI block style)</read_first>
  <action>
    1. `src/services/providers/local.provider.js` — add near the other requires: `const { contextManager } = require('../../core/context.manager');`. Update EXACTLY three lines (mdContext is the LAST param; image takes it 5th after programmingLanguage, text/transcription 5th after programmingLanguage):
    - :259 → `this.requestBuilder.buildImageRequest(imageBuffer, mimeType, activeSkill, programmingLanguage, contextManager.getContext())`
    - :287 → `this.requestBuilder.buildTextRequest(text, activeSkill, sessionMemory, programmingLanguage, contextManager.getContext())`
    - :313 → `this.requestBuilder.buildTranscriptionRequest(text, activeSkill, sessionMemory, programmingLanguage, contextManager.getContext())`
    NO other provider/serialize/RequestBuilder changes (per user decision: wire into the EXISTING seam; note in code comment: "notes loaded at launch; getContext() is a cheap cached-string read").
    2. `main.js`:
    - Top: `const { contextManager } = require('./src/core/context.manager');`
    - In `onAppReady`, alongside the other manager starts (isolated, non-blocking): `try { const s = await contextManager.load(); logger.info('Notes context loaded', s); } catch (e) { logger.warn('Notes context load failed', { error: e.message }); }`
    - New IPC handler next to `show-settings`: `ipcMain.handle('select-notes-folder', async () => { const { dialog } = require('electron'); const r = await dialog.showOpenDialog({ properties: ['openDirectory'] }); if (r.canceled || !r.filePaths.length) return { canceled: true }; return { canceled: false, path: r.filePaths[0] }; })` (plain ipcMain.handle now; 05-05 converts every handler to the guarded form).
    - `getSettings()` — add: `notesFolder: config.get('notes.folder') || '', notesBudgetChars: config.get('notes.budgetChars'), notesStatus: contextManager.getStatus(),`
    - `saveSettings(settings)` — add to the envUpdates section: `if (settings.notesFolder !== undefined) { envUpdates.NOTES_FOLDER = settings.notesFolder; }` (restart-to-apply, matching LOCAL_MODEL).
    3. `preload.js` — add to the electronAPI settings group: `selectNotesFolder: () => ipcRenderer.invoke('select-notes-folder'),`
  </action>
  <verify>
    <automated>npx eslint src/services/providers/local.provider.js main.js preload.js && make run_tests && node --test test/local-provider.test.js && timeout 25 npx electron . 2>&1 | tee /tmp/05-03-boot.log; ! grep -qi "uncaught\|unhandled" /tmp/05-03-boot.log && grep -qi "notes context" /tmp/05-03-boot.log</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "contextManager.getContext\(\)" src/services/providers/local.provider.js` → 3 (all three build* call sites)
    - `main.js` contains `select-notes-folder` and `contextManager.load()` and `envUpdates.NOTES_FOLDER`
    - `getSettings()` returns `notesFolder` + `notesStatus` keys (grep both)
    - `preload.js` contains `selectNotesFolder`
    - Headless boot logs 'Notes context loaded' (empty-folder path: loadedCount 0) with zero uncaught
    - Existing test/local-provider.test.js still green (the 3 call sites only ADD a trailing arg)
  </acceptance_criteria>
  <done>Every model call carries the loaded notes; commit `feat(05-03): wire notes context into provider + settings persistence` with pathspec `git commit -- src/services/providers/local.provider.js main.js preload.js`</done>
</task>

<task type="auto">
  <name>Task 3: Settings UI — folder picker + editable path + "N of M files loaded"</name>
  <files>settings.html, src/ui/settings-window.js</files>
  <read_first>settings.html (find the AI Model section markup added in 03-05 — mirror its section/row classes exactly), src/ui/settings-window.js (loadSettings/save flow — how fields are populated from getSettings and collected into saveSettings; the #modelStatus health-line pattern)</read_first>
  <action>
    1. `settings.html` — add a "Notes Context" section after the AI Model section, using the SAME section/label/input/button classes as existing rows:
    - Text input `id="notesFolder"` (editable path), placeholder "Folder of .md notes (optional)"
    - Button `id="notesBrowse"` labeled "Browse…"
    - Status line `id="notesStatus"` (muted style like #modelStatus)
    - Hint line: "Loaded once at launch — restart the app to apply changes."
    2. `src/ui/settings-window.js`:
    - Populate on load: `notesFolder.value = settings.notesFolder || ''`; render status from `settings.notesStatus` as `Loaded ${loadedCount} of ${totalCount} files (${chars} of ${budget} chars)` or "No notes folder configured" when folder is empty.
    - `notesBrowse` click → `const r = await window.electronAPI.selectNotesFolder(); if (!r.canceled) notesFolder.value = r.path;`
    - Include `notesFolder: notesFolder.value.trim()` in the object passed to the existing saveSettings call.
    - Degrade-never-crash: guard all DOM lookups (mirror the file's existing null-check style).
  </action>
  <verify>
    <automated>npx eslint src/ui/settings-window.js && make run_tests && rg -q "notesFolder" settings.html && rg -q "selectNotesFolder" src/ui/settings-window.js</automated>
  </verify>
  <acceptance_criteria>
    - `settings.html` contains ids `notesFolder`, `notesBrowse`, `notesStatus` and the restart hint text
    - `src/ui/settings-window.js` contains `selectNotesFolder` (picker call) and `notesFolder` in the save payload and the `of` status string
    - `make lint` 0, `make run_tests` green
  </acceptance_criteria>
  <done>Settings exposes picker + path + N-of-M status; commit `feat(05-03): notes folder settings UI` with pathspec `git commit -- settings.html src/ui/settings-window.js`</done>
</task>

</tasks>

<verification>
- `make run_tests` green (frame-dedup + sanitize-policy + context-manager suites all present); `make lint` 0
- Headless boot: 'Notes context loaded' logged, zero uncaught
- grep proves 3 provider call sites pass getContext(); no fs.watch anywhere
- End-to-end (folder → restart → answer reflects notes) is attended → 05-06 gate
</verification>

<success_criteria>
CONT-05 per locked decisions: launch-only reload, 12k configurable budget over the assembled string, whole-files alphabetical stop-before-bust, N-of-M surfaced in settings, native picker + editable path, wired through the existing mdContext seam with zero provider-shape changes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-continuous-capture-notes-hardening/05-03-SUMMARY.md` (cite 03-07's 12k prefill validation; note the new `select-notes-folder` channel for the 05-05 IPC table).
</output>
