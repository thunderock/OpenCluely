---
phase: 05-continuous-capture-notes-hardening
plan: 05
type: execute
wave: 4
depends_on: ["05-02", "05-03", "05-04"]
files_modified: [src/core/ipc-scope.js, test/ipc-scope.test.js, src/managers/window.manager.js, preload-overlay.js, main.js]
autonomous: true
requirements: [SEC-03]

must_haves:
  truths:
    - "The response/overlay renderers (llmResponse, chat) cannot read settings — get-settings/save-settings invoked from them is denied with a structured warn log"
    - "Every ipcMain channel declares its allowed window audience in ONE table; unknown channel or unknown sender ⇒ deny"
    - "Overlay/chat windows load a minimal preload that never exposes settings/model/whisper APIs (defense in depth under the main-process gate)"
    - "All legitimate flows keep working: chat sends messages + toggles speech, overlay copies/opens links/resizes, settings and onboarding unchanged"
  artifacts:
    - path: "src/core/ipc-scope.js"
      provides: "CHANNEL_AUDIENCES table (EVERY channel) + isChannelAllowed(channel, windowType) with default-deny"
      exports: ["CHANNEL_AUDIENCES", "isChannelAllowed"]
    - path: "test/ipc-scope.test.js"
      provides: "node:test: trio denial for overlay, default-deny, table completeness vs main.js registrations"
    - path: "preload-overlay.js"
      provides: "Minimal bridge for llmResponse + chat window classes"
      contains: "copyToClipboard"
  key_links:
    - from: "main.js ipcMain registrations"
      to: "src/core/ipc-scope.js"
      via: "guardedHandle/guardedOn wrappers resolving event.sender.id → window type → isChannelAllowed"
      pattern: "guardedHandle"
    - from: "src/managers/window.manager.js createWindow"
      to: "webContents.id → type registry"
      via: "webContentsTypes Map set immediately after new BrowserWindow, cleaned on destroyed"
      pattern: "webContentsTypes"
    - from: "src/managers/window.manager.js createWindow"
      to: "preload-overlay.js"
      via: "per-window-class preload override (llmResponse/chat → overlay, rest → preload.js)"
      pattern: "preload-overlay"
---

<objective>
Scope privileged IPC by sender: a central channel→allowed-window-types table enforced in the main process via `event.sender` identity (the load-bearing gate), plus per-window-class preloads so the overlay/chat renderers never even expose settings APIs. Violations are denied with a structured warn log — never a throw, never a silent null.

Purpose: SEC-03 — model-output renderers process untrusted content; if one is compromised it must not be able to read settings or exfiltrate anything. Defense-in-depth + Phase 6 future-proofing.
Output: `src/core/ipc-scope.js` (+tests), WebContents-id registry + preload split in window.manager.js, `preload-overlay.js`, guarded registrations across main.js.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-continuous-capture-notes-hardening/05-CONTEXT.md
@.planning/phases/05-continuous-capture-notes-hardening/05-RESEARCH.md
@.planning/phases/05-continuous-capture-notes-hardening/05-03-SUMMARY.md
@.planning/phases/05-continuous-capture-notes-hardening/05-04-SUMMARY.md
@preload.js
@src/managers/window.manager.js

<interfaces>
Window types (window.manager.js): `main`, `chat`, `llmResponse`, `settings`, `onboarding`. ONE shared preload today (config.js:31), spread at window.manager.js:277.

AUDITED per-renderer usage (RESEARCH "IPC surface" section = the ground truth for audiences):
- llmResponse: copy-to-clipboard, open-external (added 05-02 link routing), expand-llm-window, resize-llm-window-for-content, close-window, quit-app (legacy api.send)
- chat: send-chat-message, start-speech-recognition, stop-speech-recognition, get-speech-availability, copy-to-clipboard, open-external (05-02), chat-window-ready, close-window
- main: the full control surface incl. get/save-settings, model/speech/window channels, open-privacy-settings + relaunch-app (05-04)
- settings: get/save-settings (+legacy on), whisper + model channels, select-notes-folder (05-03), update-app-icon, update-active-skill, restart-app-for-stealth, close-settings, update-skill, set-window-gap, quit-app, close-window, test-provider-connection, list-installed-models
- onboarding: get-first-run-status, complete-first-run, open-external, close-onboarding, download-whisper-model, get-whisper-status, download-model, get-model-status, model-preflight, save-settings, list-installed-models

THE SEC-03 TRIO + Phase-5 channels — encode these rows EXACTLY:
```js
'get-settings':          ['main', 'settings', 'onboarding'],   // NEVER chat/llmResponse
'save-settings':         ['main', 'settings', 'onboarding'],
'open-external':         ['chat', 'llmResponse', 'onboarding'],// http(s)-validated handler; overlay needs it for sanitized links
'copy-to-clipboard':     ['main', 'chat', 'llmResponse'],
'open-privacy-settings': ['main'],
'relaunch-app':          ['main'],
'select-notes-folder':   ['settings'],
```
Full registration inventory to cover (RESEARCH lists every main.js line number): take-screenshot, list-displays, capture-area, get-speech-availability, start/stop-speech-recognition (handle AND on), speech-reattach-channel, audio-chunk (on), chat-window-ready (on), main-window-ready (on), test-chat-window (on), show/hide-all-windows, enable/disable-window-interaction, switch-to-chat, switch-to-skills, resize-window, move-window, get-session-history, clear-session-memory, force-always-on-top, test-always-on-top, send-chat-message, get-skill-prompt, set-window-binding, toggle-window-binding, get-window-binding-status, get-window-stats, set-window-gap, move-bound-windows, show-settings, get-first-run-status, complete-first-run, close-onboarding, download-whisper-model, get-whisper-status, whisper-recover, download-model, get-model-status, list-installed-models, model-preflight, recover-model, test-provider-connection, update-app-icon, update-active-skill, restart-app-for-stealth, close-window, expand-llm-window, resize-llm-window-for-content, quit-app (handle AND on), close-settings (on), save-settings (on), update-skill (on) + the three new Phase-5 channels.

Audience rule where the audit is silent: window-management/control channels → widen toward PRIVILEGED windows (main, settings) — NEVER toward chat/llmResponse. Outbound `webContents.send` events (permission-status, llm-response, etc.) are NOT gated (main picks targets).

Unused legacy channels found during the audit (preload references with NO main-side registration: hide-settings, get-llm-session-history, format-session-history, toggle-recording, toggle-interaction-mode, window-loaded): give them NO table entry (default-deny covers them) and FLAG in SUMMARY for Phase 8 — do NOT delete.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ipc-scope table + helper (+ completeness test against main.js)</name>
  <files>src/core/ipc-scope.js, test/ipc-scope.test.js</files>
  <read_first>main.js (grep every `ipcMain.handle(` / `ipcMain.on(` registration — the table must cover ALL of them including select-notes-folder/open-privacy-settings/relaunch-app), .planning/phases/05-continuous-capture-notes-hardening/05-RESEARCH.md (IPC audit section — per-renderer usage), preload.js (both bridges' channel lists)</read_first>
  <behavior>
    Test cases:
    - isChannelAllowed('get-settings', 'llmResponse') === false; same for 'chat' — and for 'save-settings'
    - isChannelAllowed('get-settings', 'settings') === true; ('get-settings','main') === true; ('get-settings','onboarding') === true
    - isChannelAllowed('open-external', 'llmResponse') === true; ('open-external', 'main') === false (not in audited usage)
    - isChannelAllowed('open-privacy-settings', 'main') === true; ('open-privacy-settings', 'chat') === false
    - isChannelAllowed('select-notes-folder', 'settings') === true; ('select-notes-folder', 'llmResponse') === false
    - default-deny: isChannelAllowed('made-up-channel', 'main') === false; isChannelAllowed('get-settings', null) === false; (undefined windowType) === false
    - COMPLETENESS: read main.js source (fs.readFileSync from the test), extract every channel via /(?:ipcMain|guarded[A-Za-z]*)\.?(?:handle|on|Handle|On)\(\s*["']([^"']+)["']/g — assert every extracted channel is a key of CHANNEL_AUDIENCES (fails whenever a future channel is added without declaring an audience)
  </behavior>
  <action>
    Create `src/core/ipc-scope.js` — PURE CommonJS, no imports:
    - `const CHANNEL_AUDIENCES = { ... };` — one row per channel from the full inventory above. Use the exact trio/new-channel rows from <interfaces>. For every remaining channel, set the audience from the audited usage; where unaudited, follow the rule: privileged-widen (add 'main'/'settings'), NEVER add 'chat'/'llmResponse' beyond audited use. Comment each non-obvious row with its consumer (e.g. `// chat.html:1259 sendChatMessage`).
    - `function isChannelAllowed(channel, windowType) { const a = CHANNEL_AUDIENCES[channel]; return Array.isArray(a) && typeof windowType === 'string' && a.includes(windowType); }` — default-deny on unknown channel AND unknown/null windowType.
    - `module.exports = { CHANNEL_AUDIENCES, isChannelAllowed };`
    - Top-of-file comment: "Every ipcMain channel MUST declare its audience here. New channels: add a row or the completeness test + runtime gate will deny them."
    Write the tests RED first (including the completeness reflection test), then fill the table until GREEN. (The completeness test will only fully pass after Task 3 converts main.js registrations — if written against raw `ipcMain.` patterns it passes NOW and keeps passing after the mechanical rename; use the combined regex above so both forms match.)
  </action>
  <verify>
    <automated>node --test test/ipc-scope.test.js && npx eslint src/core/ipc-scope.js test/ipc-scope.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/ipc-scope.js` exports CHANNEL_AUDIENCES + isChannelAllowed; zero requires
    - Trio rows byte-match the spec: get-settings/save-settings exclude chat+llmResponse; open-external includes chat+llmResponse+onboarding
    - Completeness test passes: every channel registered in main.js has a table row
    - `node --test test/ipc-scope.test.js` exits 0 with ≥ 8 passing tests
  </acceptance_criteria>
  <done>The full declared-audience table exists and is proven complete; commit `feat(05-05): IPC channel audience table` with pathspec `git commit -- src/core/ipc-scope.js test/ipc-scope.test.js`</done>
</task>

<task type="auto">
  <name>Task 2: WebContents registry + per-class preload split + preload-overlay.js</name>
  <files>src/managers/window.manager.js, preload-overlay.js</files>
  <read_first>src/managers/window.manager.js:266-470 (createWindow: baseOptions webPreferences spread at :276-282, BrowserWindow construction at :449 — registry insert goes IMMEDIATELY after, BEFORE loadFile), preload.js (FULL file — the overlay preload is a strict subset; copy method bodies verbatim), src/core/config.js:27-32 (the baked shared preload path), eslint.config.js (which files-block preload.js sits in — preload-overlay.js needs the same treatment)</read_first>
  <action>
    1. `src/managers/window.manager.js`:
    - Constructor: `this.webContentsTypes = new Map();`
    - In `createWindow(type, ...)`, compute the per-class preload BEFORE building options:
    ```js
    const preloadPath = (type === 'llmResponse' || type === 'chat')
      ? path.join(__dirname, '../../preload-overlay.js')
      : config.get('window.webPreferences').preload;   // preload.js (privileged: main/settings/onboarding)
    ```
    and add `preload: preloadPath` to baseOptions webPreferences AFTER the `...config.get('window.webPreferences')` spread so the override wins.
    - Immediately after `const window = new BrowserWindow(browserWindowOptions);` (BEFORE any loadFile/show — avoids the early-IPC race):
    ```js
    const wcId = window.webContents.id;
    this.webContentsTypes.set(wcId, type);
    window.webContents.on('destroyed', () => this.webContentsTypes.delete(wcId));
    ```
    - Add method: `getWindowTypeByWebContentsId(id) { return this.webContentsTypes.get(id) || null; }`
    2. Create `preload-overlay.js` (repo root, beside preload.js) — same contextBridge style; expose ONLY:
    - electronAPI: `copyToClipboard`, `openExternal`, `sendChatMessage`, `startSpeechRecognition`, `stopSpeechRecognition`, `getSpeechAvailability`, `expandLlmWindow`, `resizeLlmWindowForContent`, `closeWindow`, `notifyChatWindowReady` (→ `ipcRenderer.send('chat-window-ready')`), the receive-only events used by the two pages (`onDisplayLlmResponse`, `onShowLoading`, `onInterimTranscription`, `onLlmResponse`, `onRecordingStarted`, `onRecordingStopped`, `onSessionCleared`, `onSpeechAvailability`, `onSpeechError`, `onSpeechStatus`, `onTranscriptionLlmResponse`, `onTranscriptionLlmResponseStart`, `onTranscriptionLlmResponseChunk`, `onTranscriptionReceived`), plus `receive` and `removeAllListeners` — copy each body VERBATIM from preload.js.
    - legacy api: `send` allowlist reduced to `['quit-app', 'window-loaded']`; `receive` valid-channel list copied unchanged (receive-only is safe).
    - Header comment block EXPLICITLY listing what is absent and why: getSettings, saveSettings, selectNotesFolder, all model/whisper channels, takeScreenshot/captureArea, openPrivacySettings, relaunchApp, sendAudioChunk, window-binding/management beyond resize/expand/close (SEC-03: overlay renders untrusted model output).
    3. eslint: ensure preload-overlay.js lints under the same block as preload.js (add the filename to eslint.config.js if the block enumerates files).
  </action>
  <verify>
    <automated>npx eslint src/managers/window.manager.js preload-overlay.js && make run_tests && node -e "const s=require('fs').readFileSync('preload-overlay.js','utf8'); if(/getSettings|saveSettings|selectNotesFolder|downloadWhisperModel|pullModel|openPrivacySettings|relaunchApp|sendAudioChunk|takeScreenshot|captureArea/.test(s)){console.error('privileged API leaked into overlay preload');process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `window.manager.js` contains `webContentsTypes` (Map), `getWindowTypeByWebContentsId`, `preload-overlay` and the `destroyed` cleanup
    - Registry `.set(` happens between `new BrowserWindow` and any `loadFile`/`loadURL` call (verify by reading the diff)
    - `preload-overlay.js` exists; the leak-check node one-liner exits 0 (no privileged API names present)
    - `preload-overlay.js` legacy `api.send` allowlist is exactly `['quit-app', 'window-loaded']`
    - `make run_tests` green, `make lint` 0
  </acceptance_criteria>
  <done>Sender identity resolvable + overlay bridge minimized; commit `feat(05-05): webContents registry + per-class preload split` with pathspec `git commit -- src/managers/window.manager.js preload-overlay.js`</done>
</task>

<task type="auto">
  <name>Task 3: Guard every ipcMain registration in main.js</name>
  <files>main.js</files>
  <read_first>main.js:530-1125 (EVERY ipcMain.handle/on registration — the full mechanical surface), src/core/ipc-scope.js (the helper contract from Task 1), src/managers/window.manager.js getWindowTypeByWebContentsId (Task 2)</read_first>
  <action>
    1. Define the two wrappers ONCE near the top of the IPC setup (before the first registration):
    ```js
    const { isChannelAllowed } = require('./src/core/ipc-scope');
    const guardedHandle = (channel, fn) => ipcMain.handle(channel, (event, ...args) => {
      const windowType = windowManager.getWindowTypeByWebContentsId(event.sender && event.sender.id);
      if (!isChannelAllowed(channel, windowType)) {
        logger.warn('IPC denied', { channel, windowType: windowType || 'unknown' });
        return { ok: false, error: 'denied' };
      }
      return fn(event, ...args);
    });
    const guardedOn = (channel, fn) => ipcMain.on(channel, (event, ...args) => {
      const windowType = windowManager.getWindowTypeByWebContentsId(event.sender && event.sender.id);
      if (!isChannelAllowed(channel, windowType)) {
        logger.warn('IPC denied', { channel, windowType: windowType || 'unknown' });
        return;   // drop silently after the warn log (never throw)
      }
      fn(event, ...args);
    });
    ```
    2. Mechanically convert EVERY registration: `ipcMain.handle("x", ...)` → `guardedHandle("x", ...)` and `ipcMain.on("x", ...)` → `guardedOn("x", ...)` — ALL ~62 registrations including the Phase-5 additions (select-notes-folder, open-privacy-settings, relaunch-app). Handler bodies are untouched; only the registration call changes.
    3. Deny semantics per locked decision: handle → `{ ok: false, error: 'denied' }` + warn log; on → drop + warn log. NO throws, NO silent nulls, degrade-never-crash.
  </action>
  <verify>
    <automated>npx eslint main.js && node --test test/ipc-scope.test.js && make run_tests && node -e "const s=require('fs').readFileSync('main.js','utf8'); const raw=(s.match(/ipcMain\.(handle|on)\(/g)||[]).length; if(raw>2){console.error('unguarded ipcMain registrations remain: '+raw);process.exit(1)}" && timeout 25 npx electron . 2>&1 | tee /tmp/05-05-boot.log; ! grep -qi "uncaught\|unhandled" /tmp/05-05-boot.log; ! grep -q "IPC denied" /tmp/05-05-boot.log</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "guardedHandle\(" main.js` ≥ 50 and `rg -c "guardedOn\(" main.js` ≥ 8
    - Only the two wrapper definitions still reference `ipcMain.handle(`/`ipcMain.on(` directly (the node one-liner allows ≤ 2 raw matches)
    - Deny path returns `{ ok: false, error: 'denied' }` and warn-logs `IPC denied` with `{ channel, windowType }` metadata
    - Headless boot: zero uncaught AND zero 'IPC denied' lines (legit startup flows all pass the gate — a denied line at boot means an audience row is wrong; fix the table, not the gate)
    - ipc-scope completeness test still green after the rename
  </acceptance_criteria>
  <done>The load-bearing sender gate is live on every channel; commit `feat(05-05): sender-scoped guard on all ipcMain channels` with pathspec `git commit -- main.js`</done>
</task>

</tasks>

<verification>
- `node --test test/ipc-scope.test.js` green (incl. completeness reflection); full suite + lint green
- Headless boot clean with ZERO denied lines (false-positive check) — live overlay denial probe is attended → 05-06 gate
- grep: no raw `ipcMain.handle(`/`ipcMain.on(` outside the wrapper definitions; overlay preload leak-check passes
- Unused legacy channels flagged in SUMMARY for Phase 8 (NOT deleted)
</verification>

<success_criteria>
SEC-03 per locked decisions: BOTH layers live (main-process sender→channel allowlist covering every channel on both bridges + per-window-class preload split), overlay/chat minimal surface with zero settings access, deny+warn on violation, full channel table with declared audiences.
</success_criteria>

<output>
After completion, create `.planning/phases/05-continuous-capture-notes-hardening/05-05-SUMMARY.md` (include: final audience table stats, the Phase-8 flag list of unused legacy channels, any audience rows widened during boot testing and why).
</output>
