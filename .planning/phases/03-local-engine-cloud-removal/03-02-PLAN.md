---
phase: 03-local-engine-cloud-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - prompts/general.md
  - prompts/programming.md
  - prompts/dsa.md
  - prompt-loader.js
  - src/core/skill-normalizer.js
  - src/core/request-builder.js
  - main.js
  - settings.html
  - src/ui/settings-window.js
  - onboarding.html
  - test/request-builder.test.js
  - test/skill-normalizer.test.js
  - test/fixtures/gemini-requests/transcription.json
autonomous: true
requirements: [GEN-01]
must_haves:
  truths:
    - "With no skill overlay active, the default answer style is a concise general reply-suggester (not DSA/interview-specific)"
    - "The user can pick a skill in settings; General is the default and Coding is one selectable skill"
    - "Coding help works via the existing programming-language-injection machinery, broadened from DSA-specific to general-purpose coding"
    - "The hardcoded interview string at request-builder.js:35 is gone from every request the app builds"
    - "No 'interview' framing remains in the shipped prompts, the transcription prompt, the skill picker, or the onboarding copy"
  artifacts:
    - path: "prompts/general.md"
      provides: "The concise general reply-suggester default skill prompt"
      min_lines: 8
    - path: "prompt-loader.js"
      provides: "Loads the general + programming skills (not dsa-only); getAvailableSkills returns both"
      contains: "general"
    - path: "src/core/request-builder.js"
      provides: "getIntelligentTranscriptionPrompt rewritten general (no interview framing)"
      contains: "reply"
    - path: "settings.html"
      provides: "#activeSkill select offers General (default) + Coding"
      contains: "General"
  key_links:
    - from: "prompt-loader.js loadPrompts()"
      to: "prompts/general.md + prompts/programming.md"
      via: "whitelist filter (replaces the dsa-only continue)"
      pattern: "general"
    - from: "src/ui/settings-window.js activeSkill change"
      to: "main.js update-active-skill / update-skill IPC"
      via: "window.api.send('update-skill', value)"
      pattern: "update-skill"
---

<objective>
Generalize the skill/prompt system so the default answer is a concise general reply-suggester and DSA/coding becomes an opt-in skill overlay (GEN-01). Handle BOTH prompt sources RESEARCH Pattern 4 identifies: the `.md` skill path (`prompt-loader` → `dsa.md`) and the hardcoded transcription prompt (`request-builder.js:31-85`, incl. the interview string at line 35). Ship exactly two skills — General (default) + Coding — with no interview branding.

Purpose: The founder repositioning (CONTEXT Positioning) makes OpenCluely a general always-on copilot / auto-reply suggester. This plan is the functional half of that scrub; README (P8) and website (P9) are out of scope.

Output: a new `prompts/general.md`, a broadened `prompt-loader`/`skill-normalizer`, a rewritten general transcription prompt, a settings skill picker defaulting to General, scrubbed onboarding copy, and updated tests/goldens.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-local-engine-cloud-removal/03-CONTEXT.md
@.planning/phases/03-local-engine-cloud-removal/03-RESEARCH.md
@prompt-loader.js
@src/core/skill-normalizer.js
@src/core/request-builder.js
@prompts/dsa.md
@prompts/programming.md
@test/request-builder.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add general.md, broaden the skill loader + normalizer, reframe the coding skill</name>
  <files>prompts/general.md, prompts/programming.md, prompts/dsa.md, prompt-loader.js, src/core/skill-normalizer.js</files>
  <action>
Implement the `.md`-skill half of GEN-01 (RESEARCH Pattern 4):

1. **Create `prompts/general.md`** — the concise reply-suggester that becomes the default skill. Use this content (RESEARCH draft, Claude's discretion approved) as an `.md` prompt:
   - Title: "General Copilot — Concise Reply Suggester" (NO "interview").
   - Body: "You are a concise, private copilot that suggests what to say or do next. Given the user's screen, the conversation, and their notes, reply with a short, ready-to-use suggestion — the actual words to say or the direct answer — not meta-commentary. Default to 1–3 sentences or a tight list; expand only when the question clearly needs depth (e.g., a coding problem or a detailed explanation). Never mention that you are an AI or that you are assisting. If nothing useful can be added, say nothing of substance."

2. **Broaden `prompt-loader.js`**:
   - Replace the dsa-only filter at `prompt-loader.js:32` (`if (skillName !== 'dsa') continue;`) with a whitelist load of `['general','programming']` (skip other `.md`).
   - Change `getAvailableSkills()` (currently hard-returns `['dsa']` at `prompt-loader.js:308`) to return `['general','programming']`.

3. **`src/core/skill-normalizer.js`** (RESEARCH Open Q3 — fold DSA into general coding, keep an alias so old refs don't break):
   - In the `skillMap` (skill-normalizer.js:18-50) map `'dsa'`, `'data-structures'`, `'algorithms'`, `'data-structures-algorithms'` → `'programming'` (was `'dsa'`).
   - Change `SKILLS_REQUIRING_PROGRAMMING_LANGUAGE` (skill-normalizer.js:8) from `['dsa']` to `['programming']`.
   - In `injectProgrammingLanguage()` (skill-normalizer.js:57-83) rename the `case 'dsa':` to `case 'programming':` so the coding language-injection block still fires for the coding skill (keep the block content — it is generic coding guidance).

4. **Reframe `prompts/programming.md`** from "Programming Interview Helper Agent" → a general-purpose coding assistant (keep the response structure; drop "interview" wording, e.g. line 1 title, line 24 "interview-ready implementation"). This is now the Coding skill.

5. **Retire `prompts/dsa.md`**: since `dsa` now normalizes to `programming`, `dsa.md` is no longer loaded. Either delete it OR fold its useful DSA guidance into `programming.md` as an "algorithmic problems" section. Do NOT leave "DSA Interview Helper" title text shipping (prompts/dsa.md:1). Prefer deletion (the `dsa` alias still resolves to `programming.md`).

Match existing conventions: CommonJS, no new deps, the `assests/` misspelling is unrelated here.
  </action>
  <verify>
`node -e "const {promptLoader}=require('./prompt-loader'); console.log(promptLoader.getAvailableSkills()); console.log(!!promptLoader.getSkillPrompt('general')); console.log(!!promptLoader.getSkillPrompt('dsa'))"` prints `[ 'general', 'programming' ]`, `true`, `true` (dsa alias → programming.md). `grep -ri interview prompts/ prompt-loader.js src/core/skill-normalizer.js` returns nothing. `npx eslint prompt-loader.js src/core/skill-normalizer.js` exits 0.
  </verify>
  <done>general.md exists; loader/normalizer load general+programming with dsa aliased to programming; coding language-injection still fires; no "interview" text in prompts or skill machinery.</done>
</task>

<task type="auto">
  <name>Task 2: Neutralize the transcription prompt + flip skill defaults + scrub onboarding copy</name>
  <files>src/core/request-builder.js, main.js, settings.html, src/ui/settings-window.js, onboarding.html</files>
  <action>
Handle the SECOND prompt source and the user-facing defaults:

1. **`src/core/request-builder.js` `getIntelligentTranscriptionPrompt()` (lines 31-85)** — the #1 functional interview-ism. Rewrite toward a general concise reply-suggester:
   - DELETE the interview line at `request-builder.js:35` ("Assume you are in an interview and you need to perform best in `${activeSkill}` mode.").
   - Rewrite the prompt to: default to short, ready-to-say suggestions; expand only when the question clearly needs depth; drop DSA-centricity and all "interview" framing; keep it skill-aware (still parameterized by `activeSkill`) so the Coding skill still gets coding behavior.
   - KEEP the `programmingLanguage` code-fence injection block (request-builder.js:38-46) — it is coding context, not interview framing.
   - This method is pure/DI-tested; keep it deterministic (no randomness) so tests stay stable.

2. **`main.js` default skill** — change the DSA default to General:
   - Constructor `this.activeSkill = "dsa"` (main.js:96) → `"general"`.
   - `getSettings()` default `activeSkill: this.activeSkill || "dsa"` (main.js:1475) → `|| "general"`.

3. **Settings skill picker** — `settings.html` `#activeSkill` select (settings.html:319-321) currently offers only `<option value="dsa">Data Structures & Algorithms</option>`. Replace with:
   - `<option value="general">General</option>` (default)
   - `<option value="programming">Coding</option>`
   `settings-window.js` already wires `#activeSkill` (loads at line 92, saves at 143, broadcasts via `window.api.send('update-skill', ...)` at 222) — no logic change needed; verify the default General is preselected when settings.activeSkill is 'general'.

4. **Scrub onboarding interview copy** in `onboarding.html`:
   - Line 727: "Your AI-powered interview & problem-solving copilot." → reposition to the general copilot / auto-reply-suggester framing (e.g., "Your private always-on copilot — it watches your screen, hears the conversation, and suggests what to say next.").
   - Line 926: "If this saved you time in an interview or a coding session…" → neutral general wording.
   Leave the onboarding Gemini apikey/speech screens alone here (Gemini apikey screen is removed at PROV-07; Ollama/model screens are added in 03-06).
  </action>
  <verify>
`grep -n "interview" src/core/request-builder.js main.js onboarding.html` returns nothing (case-insensitive: `grep -ni interview ...`). `node -e "const {RequestBuilder}=require('./src/core/request-builder'); const rb=new RequestBuilder({sessionManager:{},promptLoader:{getSkillPrompt:()=>null,getRequestComponents:()=>({})}}); const p=rb.getIntelligentTranscriptionPrompt('general',null); console.log(!/interview/i.test(p))"` prints `true`. `grep -n 'value="general"' settings.html` matches. `npx eslint src/core/request-builder.js main.js` exits 0.
  </verify>
  <done>request-builder.js:35 interview string gone; transcription prompt is general concise reply-suggester; main.js defaults to 'general'; settings offers General(default)+Coding; onboarding copy repositioned.</done>
</task>

<task type="auto">
  <name>Task 3: Update tests + regenerate the Gemini transcription golden</name>
  <files>test/request-builder.test.js, test/skill-normalizer.test.js, test/fixtures/gemini-requests/transcription.json</files>
  <action>
GEN-01 intentionally changes the transcription prompt, so the Phase-2 parity assets shift (CONTEXT: expected, not a regression — they are deleted with Gemini at PROV-07). Keep the suite green coherently:

1. **`test/request-builder.test.js`** — the transcription tests assert `r.systemPrompt.includes('Intelligent Transcription Response System')` (lines 182, 219). If your rewrite keeps that H1 title, they pass unchanged; if you renamed the title, update these two assertions to the new stable marker. Add one assertion that the transcription systemPrompt does NOT match `/interview/i`. Do NOT weaken the history-cap / role-mapping / mdContext tests.

2. **`test/skill-normalizer.test.js`** — update any expectation that `normalizeSkillName('dsa') === 'dsa'` to `=== 'programming'`, and that `SKILLS_REQUIRING_PROGRAMMING_LANGUAGE` is `['programming']`. (Read the file first; adjust only the assertions the alias/whitelist change affects.)

3. **Regenerate the Gemini transcription golden**: run `node scripts/capture-gemini-goldens.js`. Its transcription fixture embeds the real `getIntelligentTranscriptionPrompt` output, so it now reflects the general prompt. Confirm `test/fixtures/gemini-requests/transcription.json` changed and `test/gemini-request-parity.test.js` passes against the regenerated golden (text.json/image.json use faked prompts and should NOT change). Note in the SUMMARY that this golden set is retired at PROV-07.
  </action>
  <verify>
`node --test test/*.test.js` is fully green (request-builder, skill-normalizer, gemini-request-parity included). `git diff --stat test/fixtures/gemini-requests/` shows only `transcription.json` changed (not text/image). `npx eslint .` exits 0.
  </verify>
  <done>All node:test suites pass with the generalized prompt; only the transcription golden regenerated; interview-string absence asserted.</done>
</task>

</tasks>

<verification>
- `grep -rni interview prompts/ prompt-loader.js src/core/skill-normalizer.js src/core/request-builder.js main.js onboarding.html settings.html` returns nothing.
- `promptLoader.getAvailableSkills()` → `['general','programming']`; `getSkillPrompt('general')` and `getSkillPrompt('dsa')` (alias) both non-null.
- Default active skill is 'general' (main.js constructor + getSettings).
- `node --test test/*.test.js` green; `npx eslint .` exits 0.
</verification>

<success_criteria>
GEN-01 satisfied: general concise reply-suggester default; General(default)+Coding skill picker; coding machinery broadened DSA→general via the alias; both prompt sources de-interviewed; tests + transcription golden coherent.
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-engine-cloud-removal/03-02-SUMMARY.md`.
</output>
