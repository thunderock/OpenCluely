---
phase: 03-local-engine-cloud-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/core/config.js
autonomous: true
requirements: [PROV-06]
must_haves:
  truths:
    - "The two CJS-safe transport deps (openai, ollama) are installed and require() without throwing"
    - "config exposes a provider-selection key (llm.provider) that defaults to 'local' and can be overridden by LLM_PROVIDER"
    - "config exposes a per-provider llm.local block (host, model, keepAlive, curatedModels) and STILL keeps the llm.gemini block intact"
    - "The app still runs on Gemini after this plan (registry selection is unchanged here — only the config shape grows)"
  artifacts:
    - path: "package.json"
      provides: "openai + ollama dependencies added; @google/genai still present (removed later at PROV-07)"
      contains: "\"openai\""
    - path: "src/core/config.js"
      provides: "llm.provider selector + llm.local per-provider block; llm.gemini preserved"
      contains: "provider:"
  key_links:
    - from: "src/core/config.js"
      to: "process.env.LLM_PROVIDER / OLLAMA_BASE_URL / LOCAL_MODEL"
      via: "env overrides read in loadConfiguration()"
      pattern: "process\\.env\\.LLM_PROVIDER"
---

<objective>
Lay the Phase-3 foundation both new modules depend on: install the two CJS-safe transport dependencies and restructure the `llm` config into per-provider blocks with a provider-selection key (PROV-06, config half).

Purpose: `LocalProvider` (03-03) needs `openai` + `config.llm.local`; `LocalModelManager` (03-04) needs `ollama` + `config.llm.local`. Doing this once, first, lets both build in parallel without touching `package.json` or `config.js` again. Critically, this plan does NOT flip the provider registry — the app keeps running on Gemini until Local is registered in 03-03, honoring the load-bearing "never removal-first / keep the proven path working" sequencing.

Output: `openai` + `ollama` in `package.json`; a restructured `config.llm` with `provider`, `local`, and the preserved `gemini` block.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-local-engine-cloud-removal/03-CONTEXT.md
@.planning/phases/03-local-engine-cloud-removal/03-RESEARCH.md
@src/core/config.js
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install openai + ollama (CJS-safe transport deps)</name>
  <files>package.json, package-lock.json</files>
  <action>
Run `npm install openai ollama` from the repo root. Per RESEARCH Flag 5 these are the locked transport choices: `openai` (^6.x) for LocalProvider inference over `/v1`, `ollama` (^0.6.x) for LocalModelManager lifecycle (pull/ps/list). Both ship a `require()` build.

Do NOT add `get-port@7`, `execa@9`, or `node-fetch@3` — all ESM-only and throw on `require()` in this CJS app (CONTEXT Specifics; RESEARCH CJS/ESM guardrails). Use built-in `node:child_process`, global `fetch`, and `net` where port/spawn plumbing is needed.

Do NOT touch `@google/genai` or `microsoft-cognitiveservices-speech-sdk` here — both stay this phase (Gemini removed later at PROV-07; Azure STT deferred to Phase 4).
  </action>
  <verify>
`node -e "require('openai'); require('ollama'); console.log('require-ok')"` prints `require-ok` with no throw. `grep -E '"openai"|"ollama"' package.json` shows both. `git diff package-lock.json` shows the lockfile updated. `npm ci --dry-run` reports lockfile parity (Makefile `setup` uses `npm ci`).
  </verify>
  <done>Both deps installed, require-safe, and recorded in package.json + package-lock.json; no ESM-only deps added; @google/genai and the Azure SDK untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure config.llm into per-provider blocks + a provider selector</name>
  <files>src/core/config.js</files>
  <action>
In `loadConfiguration()` (config.js:11), replace the flat `llm: { gemini: {...} }` (currently config.js:41-57) with the per-provider shape from RESEARCH Pattern 5. Keep the existing `gemini` block byte-for-byte (it is still selectable during the Phase-3 transition window and is deleted only at PROV-07):

```js
llm: {
  // Provider selection (PROV-06). Default Local; overridable via env so the
  // transition window can flip back to Gemini without a code change.
  provider: process.env.LLM_PROVIDER || 'local',

  // Per-provider block for the local engine. `host` is the client base URL
  // (scheme included); LocalProvider appends '/v1', LocalModelManager derives
  // the daemon's OLLAMA_HOST ('host:port', no scheme) from it.
  local: {
    host: process.env.OLLAMA_BASE_URL || 'http://127.0.0.1:11434',
    model: process.env.LOCAL_MODEL || 'qwen3-vl:8b',
    keepAlive: -1,
    curatedModels: ['qwen3-vl:8b', 'qwen3-vl:30b', 'gemma3:4b', 'gemma3:12b']
  },

  gemini: { /* KEEP the existing block verbatim — removed at PROV-07 */ }
}
```

Rules:
- Preserve the `gemini` block exactly as it is today (model, fallbackModels, maxRetries, timeout, fallbackEnabled, enableFallbackMethod, generation{...}). The Phase-2 golden-parity test asserts `config.get('llm.gemini.generation')` — it must keep resolving unchanged.
- `provider` default is `'local'` but the registry (index.js) is NOT edited here, so `providers.getSelected()` still returns Gemini until 03-03 wires the registry to read this key. This keeps every intermediate state working on the proven path.
- Do NOT touch the `speech.*` block (Azure STT stays until Phase 4). Leave the dead `speech.provider:'azure'` default alone to keep the Azure/STT surface isolated for its own Phase-4 removal.
- `config` is a singleton constructed at module load; env is read at construction (after dotenv). Match the existing `this.env = process.env.NODE_ENV` pattern for the env reads.
  </action>
  <verify>
`node -e "const c=require('./src/core/config'); console.log(c.get('llm.provider'), c.get('llm.local.model'), c.get('llm.local.host'), !!c.get('llm.gemini.generation'))"` prints `local qwen3-vl:8b http://127.0.0.1:11434 true`. `LLM_PROVIDER=gemini node -e "console.log(require('./src/core/config').get('llm.provider'))"` prints `gemini`. `npx eslint src/core/config.js` exits 0. `node --test test/*.test.js` stays green (Gemini parity still resolves `llm.gemini.generation`).
  </verify>
  <done>config.llm has provider/local/gemini; provider defaults to 'local' with LLM_PROVIDER override; the gemini block and generation config are preserved; speech block untouched; existing tests green.</done>
</task>

</tasks>

<verification>
- `require('openai')` and `require('ollama')` both succeed.
- `config.get('llm.provider')` === 'local' (default); `config.get('llm.local.model')` === 'qwen3-vl:8b'; `config.get('llm.gemini.generation')` still resolves.
- Registry selection unchanged → `require('./src/services/llm.service')` still resolves to GeminiProvider (app still on the proven path).
- `npx eslint .` exits 0; `node --test test/*.test.js` all green.
</verification>

<success_criteria>
Both transport deps installed and require-safe; config restructured into per-provider blocks with a Local-default selector; Gemini config preserved for the transition window; no registry/behavior flip yet.
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-engine-cloud-removal/03-01-SUMMARY.md`.
</output>
