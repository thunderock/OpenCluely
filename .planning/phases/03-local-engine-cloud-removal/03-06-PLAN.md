---
phase: 03-local-engine-cloud-removal
plan: 06
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - onboarding.html
  - onboarding.js
  - src/ui/main-window.js
autonomous: true
requirements: [PROV-05]
must_haves:
  truths:
    - "On first run, if Ollama is missing the onboarding guides the user to install it (link/instructions), openwhispr-style; it does not bundle or silently fail"
    - "On first run the onboarding auto-pulls qwen3-vl:8b with a visible progress bar (percent + status), reusing the whisper download-progress plumbing"
    - "A preflight disk/RAM warning is shown before the pull, then the flow proceeds (friendly failure)"
    - "When Local can't answer (Ollama down / model missing / OOM), the overlay shows an inline 'Local model unavailable' message with a one-click recovery keyed off owned-vs-adopted"
  artifacts:
    - path: "onboarding.html"
      provides: "An Ollama guide-install screen + a model-pull screen with a progress bar"
      contains: "ollama"
    - path: "onboarding.js"
      provides: "detect Ollama → guide vs pull; auto-pull qwen3-vl:8b via pullModel + onModelPullProgress; preflight warn"
      contains: "pullModel"
    - path: "src/ui/main-window.js"
      provides: "Local-down inline message + one-click recovery (restart/re-pull/open-settings) keyed off getModelStatus"
      contains: "Local model unavailable"
  key_links:
    - from: "onboarding.js"
      to: "electronAPI.pullModel + electronAPI.onModelPullProgress"
      via: "auto-pull with structured progress rendered as a bar"
      pattern: "onModelPullProgress"
    - from: "src/ui/main-window.js recovery"
      to: "electronAPI.getModelStatus + recoverModel"
      via: "actions keyed off owned/adopted"
      pattern: "recoverModel"
---

<objective>
Deliver the model-availability UX (PROV-05 first-run + sole-engine recovery): a first-run onboarding flow that guide-installs Ollama and auto-pulls `qwen3-vl:8b` with visible resumable progress + a preflight warning, and an in-overlay "Local model unavailable" recovery affordance for when the sole engine is down. Both are pure renderer work consuming the 03-04 IPC surface.

Purpose: The locked first-run decisions (guide-install, auto-pull up front, progress in onboarding, preflight warn) and the post-removal Local-down UX (error + one-click recovery). Because Local becomes the only engine after PROV-07, recovery must be first-class, keyed off owned-vs-adopted so the app never offers to restart a daemon it doesn't own.

Output: new onboarding screens (`onboarding.html`/`onboarding.js`) and the Local-down recovery UI in `src/ui/main-window.js`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-local-engine-cloud-removal/03-CONTEXT.md
@.planning/phases/03-local-engine-cloud-removal/03-RESEARCH.md
@onboarding.html
@onboarding.js
@preload.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Onboarding — Ollama guide-install screen + auto-pull screen with progress</name>
  <files>onboarding.html, onboarding.js</files>
  <action>
Add two onboarding screens mirroring the existing whisper install + `model-download` screens (onboarding.html:842 `data-screen="whisper"`, :871 `data-screen="model-download"`; onboarding.js screen list/order at :119, progress plumbing at :274-316 and :367-442).

1. **`onboarding.html`** — add two `<section class="screen" data-screen="...">` blocks (reuse the `.install-card`/`.install-title`/`.install-list`/`.install-log` styles):
   - `data-screen="ollama"`: "Set up your local model engine". Shows detection state; if Ollama is missing, shows guide-install instructions + an "Open ollama.com/download" button (`openExternal`) and a "Re-check" button (openwhispr-style). If present/running, shows "Ollama detected" and advances.
   - `data-screen="model-pull"`: "Downloading qwen3-vl:8b (~6 GB, one-time)". A progress bar (`id="modelPullBar"`) + status label (`id="modelPullStatus"`) + a log area (`id="modelPullLog"`). Include a preflight-warning banner slot (`id="modelPreflightWarn"`).

2. **`onboarding.js`**:
   - Add `'ollama'` and `'model-pull'` to the screen order (insert after the speech/whisper screens; the Gemini `apikey` screen stays for now — removed at PROV-07). Extend the `state` object with `ollamaDetected`, `modelPulling`, `modelPulled` (mirror `modelDownloading`/`modelDownloaded` at onboarding.js:52-53).
   - On entering `ollama`: `const s = await window.electronAPI.getModelStatus()` (or a `detect`) — if server up → mark detected + allow next; else render the guide-install instructions and wire the "Open download page" (`window.electronAPI.openExternal('https://ollama.com/download')`) + "Re-check" (re-run getModelStatus).
   - On entering `model-pull`: first `const pf = await window.electronAPI.modelPreflight()` and render any `pf.warnings` into `#modelPreflightWarn` (warn, do not block). Then auto-start the pull: subscribe via `window.electronAPI.onModelPullProgress((p) => { render percent → #modelPullBar; p.status → #modelPullStatus; append → #modelPullLog })`, call `await window.electronAPI.pullModel('qwen3-vl:8b')`, and on `{ok:true}` mark `modelPulled` + advance. Unsubscribe on leave (return the unsub fn like the whisper flow does). Handle offline/failure with a friendly retry (the pull resumes completed layers).
   - Keep the existing whisper/speech screens working (STT onboarding stays).

Match the renderer conventions already in onboarding.js (vanilla, `$`/`$$` helpers, `window.electronAPI`).
  </action>
  <verify>
`npx eslint onboarding.js` exits 0. `grep -nE 'data-screen="ollama"|data-screen="model-pull"|modelPullBar|modelPullStatus|modelPreflightWarn' onboarding.html` shows the new screens. `grep -nE "getModelStatus|modelPreflight|pullModel|onModelPullProgress|ollama.com/download" onboarding.js` shows the wiring. Manual (03-07): first-run shows guide-install when Ollama absent, and a real percent progress bar during the qwen3-vl:8b pull.
  </verify>
  <done>Onboarding guide-installs Ollama when missing and auto-pulls qwen3-vl:8b with a visible resumable progress bar + preflight warning, reusing the whisper progress plumbing; STT onboarding intact.</done>
</task>

<task type="auto">
  <name>Task 2: In-overlay "Local model unavailable" + one-click recovery</name>
  <files>src/ui/main-window.js</files>
  <action>
Add a Local-down recovery affordance in the overlay (RESEARCH Pattern 7). This is the sole-engine safety net (after PROV-07 there is no cloud fallback until Phase 7).

- Add a method (e.g. `showLocalUnavailable(status)`) on the main-window UI object that renders an inline, dismissible "Local model unavailable" panel into the overlay (reuse the existing overlay DOM/render helpers in `src/ui/main-window.js`; do NOT introduce a framework). The message + actions are keyed off `status` from `window.electronAPI.getModelStatus()`:
  - `owned && !serverUp` → primary action "Restart Ollama" → `window.electronAPI.recoverModel('restart')`.
  - `serverUp && !modelPresent` → "Re-download model" → `window.electronAPI.recoverModel('repull')` (subscribe to `onModelPullProgress` to show progress inline).
  - `adopted && !serverUp` → do NOT offer restart (the app doesn't own the daemon); show "Your Ollama isn't running — start it, or Open Settings" → `window.electronAPI.showSettings()`.
  - default/`!modelResponds` (e.g. OOM) → "Open Settings" + a short hint.
- Trigger it when a Local request fails: the transcription/text/image handlers in main.js already fall back to `generateIntelligentFallbackResponse` (03-03) whose canned text is model-availability oriented; when the overlay receives that fallback response (or an `llm-error`), call `getModelStatus()` and show this panel so the user gets a one-click path instead of a dead overlay. Wire it to the existing overlay response/error render path (find the current `onLlmError`/response handler in main-window.js and branch to `showLocalUnavailable` when the response is the local-unavailable fallback).
- Keep it CommonJS/vanilla; renderer globals (`window.electronAPI`) as used elsewhere in the file.

Do NOT remove the orphaned Gemini config modal here (that is PROV-07 / 03-08) — just add the Local-down UI.
  </action>
  <verify>
`npx eslint src/ui/main-window.js` exits 0. `grep -nE "Local model unavailable|getModelStatus|recoverModel|showLocalUnavailable" src/ui/main-window.js` shows the affordance + the owned/adopted branching. Manual (03-07): stop Ollama, ask a question → overlay shows "Local model unavailable" with a working recovery button (Restart when owned; Open Settings when adopted).
  </verify>
  <done>Overlay shows an inline Local-down message with a one-click recovery that respects owned-vs-adopted (never offers to restart an adopted daemon) and reuses the model IPC.</done>
</task>

</tasks>

<verification>
- First run: Ollama-missing → guide-install; Ollama-present → auto-pull qwen3-vl:8b with a visible percent bar + preflight warning; resumable on interruption.
- Local-down: overlay shows "Local model unavailable" + a one-click recovery keyed off owned/adopted.
- `npx eslint .` exits 0; STT onboarding + existing screens still work.
- Runtime behaviors are confirmed in the 03-07 validation smoke (needs a real Ollama).
</verification>

<success_criteria>
The locked first-run model setup (guide-install + auto-pull + progress + preflight warn) and the sole-engine Local-down recovery UX are in place, both consuming the 03-04 IPC surface.
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-engine-cloud-removal/03-06-SUMMARY.md`.
</output>
