---
phase: 03-local-engine-cloud-removal
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - settings.html
  - src/ui/settings-window.js
  - main.js
autonomous: true
requirements: [PROV-06]
must_haves:
  truths:
    - "The user can choose the LLM provider in settings, with Local as the default selection"
    - "The user can choose the model: a curated dropdown (qwen3-vl:8b default, qwen3-vl:30b, gemma3:4b, gemma3:12b) plus an advanced 'any installed' option populated from the running Ollama"
    - "Gemini remains a selectable provider during this transition window (until deletion is approved at PROV-07)"
    - "The provider/model selection persists (to .env/config) and is applied on next launch, consistent with the app's existing restart-to-apply settings"
    - "The Model section shows adopted-vs-owned + health status and a working test-connection action"
  artifacts:
    - path: "settings.html"
      provides: "Provider picker (Local default, Gemini) + model picker (curated + advanced) sections"
      contains: "provider"
    - path: "src/ui/settings-window.js"
      provides: "load/save wiring for provider + model; populate advanced list from listInstalledModels()"
      contains: "provider"
    - path: "main.js"
      provides: "getSettings surfaces provider/model/curatedModels; saveSettings persists LLM_PROVIDER + LOCAL_MODEL"
      contains: "LLM_PROVIDER"
  key_links:
    - from: "src/ui/settings-window.js"
      to: "main.js get-settings / save-settings"
      via: "getSettings()/saveSettings() include provider + model"
      pattern: "provider"
    - from: "main.js saveSettings()"
      to: ".env (LLM_PROVIDER / LOCAL_MODEL)"
      via: "persistEnvUpdates(envUpdates)"
      pattern: "LLM_PROVIDER"
---

<objective>
Add the provider + model selection UI (PROV-06): a provider picker defaulting to Local (with Gemini still selectable during the transition window), and a model picker offering the curated list plus an advanced "any installed" option queried from the running Ollama. Persist the selection and surface a Model status/health section.

Purpose: PROV-06's user-facing half. The config half + the model-list/status/test IPC already exist (03-01, 03-04); this plan is the settings renderer + the getSettings/saveSettings glue. Keeping a minimal provider switcher (even with just Local after removal) means Phase 7's CLI providers slot in with no UI rework.

Output: provider + model + Model-status sections in `settings.html`, their wiring in `settings-window.js`, and provider/model persistence in `main.js`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-local-engine-cloud-removal/03-CONTEXT.md
@.planning/phases/03-local-engine-cloud-removal/03-RESEARCH.md
@settings.html
@src/ui/settings-window.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Provider + Model + Model-status sections in settings.html</name>
  <files>settings.html</files>
  <action>
Add a new "AI Model" settings-section (match the existing `settings-section` / `settings-section-title` / `settings-item` markup already used for "Gemini Settings" at settings.html:427 and the speech section at 358). Place it ABOVE the existing "Gemini Settings" section. Contents:

1. **Provider picker** — a `<select class="input-field" id="llmProvider">` with:
   - `<option value="local">Local (Ollama)</option>` (default)
   - `<option value="gemini">Gemini (cloud — being removed)</option>`
   Add a description noting Local is the default and Gemini is available only during the transition.

2. **Model picker** (shown when provider = local):
   - Wrap in `<div id="localModelFields" class="settings-group">`.
   - Curated `<select class="input-field" id="localModel">` with options from `llm.local.curatedModels`: `qwen3-vl:8b` (default), `qwen3-vl:30b`, `gemma3:4b`, `gemma3:12b`, plus a final `<option value="__advanced__">Advanced: any installed model…</option>`.
   - An `<select class="input-field" id="localModelAdvanced" style="display:none">` (populated at runtime from `listInstalledModels()`) shown only when `__advanced__` is chosen.
   - A "Test connection" `<button id="testProviderBtn">` + a `<div id="providerStatus">` status line.

3. **Model status section** — a small block (`id="modelStatus"`) that will show adopted-vs-owned + health (server up / model present / model responds) rendered by settings-window.js. Include a "Re-download / repair model" `<button id="repairModelBtn">` (calls pullModel) and reuse the existing whisper install-log style (`install-log`) for progress lines (`id="modelStatusLog"`).

Keep the existing Gemini Settings section intact (it is removed at PROV-07). Do NOT touch the Speech/Azure sections. Match the existing class names + the `assests/` asset convention.
  </action>
  <verify>
`grep -nE 'id="llmProvider"|id="localModel"|id="localModelAdvanced"|id="testProviderBtn"|id="modelStatus"|id="repairModelBtn"' settings.html` shows all ids. The Gemini Settings section (settings.html Gemini block) is still present. `grep -c 'value="local"' settings.html` ≥ 1. HTML remains well-formed (no unclosed sections).
  </verify>
  <done>settings.html has a Provider picker (Local default + Gemini), a curated+advanced model picker, and a Model-status/repair block; Gemini + Azure sections untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire provider/model load, save, advanced-list, status, test in settings-window.js</name>
  <files>src/ui/settings-window.js</files>
  <action>
Extend `src/ui/settings-window.js` (renderer script; mirror the existing element-ref + load/save pattern at lines 6-20, 73-108, 131-146):

1. Add element refs: `llmProvider`, `localModel`, `localModelAdvanced`, `localModelFields`, `testProviderBtn`, `providerStatus`, `modelStatus`, `repairModelBtn`, `modelStatusLog`.
2. In `loadSettingsIntoUI(settings)`: set `llmProvider.value = settings.provider || 'local'`; set `localModel.value = settings.model` if it is one of the curated options, else select `__advanced__` and show `localModelAdvanced`. Toggle `localModelFields` visibility on provider === 'local' (mirror `updateSpeechFieldStates()`).
3. In `saveSettings()`: include `settings.provider = llmProvider.value` and `settings.model = (localModel.value === '__advanced__' ? localModelAdvanced.value : localModel.value)`. Keep the existing `window.api.send('save-settings', settings)` path.
4. **Advanced list**: when `localModel` changes to `__advanced__`, call `window.electronAPI.listInstalledModels()` and populate `localModelAdvanced` options; show it. (Uses the 03-04 bridge.)
5. **Provider change**: on `llmProvider` change, toggle `localModelFields`, save, and show a note that the provider switch applies on next launch (consistent with the app's existing restart-to-apply changes — e.g. the stealth-name relaunch). Do NOT attempt a live hot-swap (the facade resolves the provider at module load).
6. **Test + status**: on `testProviderBtn` click → `window.electronAPI.testProviderConnection()` → render `{success}` into `providerStatus`. On window shown (and on a light interval or manual refresh), call `window.electronAPI.getModelStatus()` and render into `modelStatus` a human line keyed off `adopted`/`owned` + the three health levels ("Using your running Ollama" (adopted) vs "Managed by OpenCluely" (owned); server up / model present / responds). `repairModelBtn` → `window.electronAPI.pullModel()` and subscribe to `onModelPullProgress` to append lines to `modelStatusLog` (mirror the whisper `onInstallProgress` append at settings/onboarding).

Renderer conventions: this file is an eslint `script` env — use the app-injected globals (`window.electronAPI`, `window.api`) already used here; no new imports.
  </action>
  <verify>
`npx eslint src/ui/settings-window.js` exits 0 (renderer globals resolve). `grep -nE "llmProvider|localModel|listInstalledModels|getModelStatus|testProviderConnection|onModelPullProgress" src/ui/settings-window.js` shows the wiring. Manual (03-07): open settings → provider shows Local; switching to advanced populates from the running Ollama; test-connection returns success against a live daemon.
  </verify>
  <done>settings-window.js loads/saves provider+model, populates the advanced list from Ollama, renders adopted/owned + health status, and wires test-connection + repair; provider switch documented as restart-to-apply.</done>
</task>

<task type="auto">
  <name>Task 3: Surface + persist provider/model in main.js getSettings/saveSettings</name>
  <files>main.js</files>
  <action>
Extend the settings bridge (do NOT touch the Azure/whisper fields — STT stays):

1. **`getSettings()` (main.js:1468-1492)**: add
   - `provider: config.get('llm.provider')`
   - `model: config.get('llm.local.model')`
   - `curatedModels: config.get('llm.local.curatedModels')`
   Keep `geminiKey` for now (removed at PROV-07).
2. **`saveSettings()` (main.js:1494+)**: in the `envUpdates` block (main.js:1525-1549), persist:
   - `if (settings.provider === 'local' || settings.provider === 'gemini') envUpdates.LLM_PROVIDER = settings.provider;`
   - `if (settings.model !== undefined) envUpdates.LOCAL_MODEL = settings.model;`
   These flow through the existing `persistEnvUpdates(envUpdates)` → `.env`, so they survive restart and are read by `config` on next launch (config reads `LLM_PROVIDER`/`LOCAL_MODEL` per 03-01). Add a `logger.info('LLM provider/model updated', { provider: settings.provider, model: settings.model })` (meta only). Note in a comment that the provider switch applies on next launch (the facade resolves the selected provider at module load), matching the existing restart-to-apply settings.
   - Do NOT wire a live provider hot-swap; do NOT change the Gemini-key reinit block yet (it is removed at PROV-07).
  </action>
  <verify>
`npx eslint main.js` exits 0. `grep -n "LLM_PROVIDER\|LOCAL_MODEL\|curatedModels" main.js` shows the additions. `node -e "const c=require('./src/core/config'); console.log(c.get('llm.provider'), c.get('llm.local.curatedModels').length)"` prints `local 4`. Round-trip (03-07): saving provider=gemini writes LLM_PROVIDER=gemini to .env; relaunch selects Gemini.
  </verify>
  <done>getSettings surfaces provider/model/curatedModels; saveSettings persists LLM_PROVIDER + LOCAL_MODEL to .env for next-launch application; Azure/whisper/Gemini-key plumbing untouched.</done>
</task>

</tasks>

<verification>
- Settings shows a provider picker (Local default, Gemini selectable) and a model picker (curated + advanced-any-installed) + a Model status/health + test/repair block.
- Selection persists to .env (LLM_PROVIDER / LOCAL_MODEL) and applies on next launch.
- Advanced list populates from the live Ollama; test-connection + status reflect the running daemon (verified in 03-07).
- `npx eslint .` exits 0; `node --test test/*.test.js` green (no test regressions).
</verification>

<success_criteria>
PROV-06 UI complete: choose provider (Local default) and model (curated + advanced) in settings, Gemini still selectable this phase, selection persisted; minimal provider switcher retained for Phase 7.
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-engine-cloud-removal/03-05-SUMMARY.md`.
</output>
