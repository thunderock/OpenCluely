---
phase: 03-local-engine-cloud-removal
plan: 08
type: execute
wave: 5
depends_on: ["03-07"]
autonomous: false
files_modified:
  - package.json
  - package-lock.json
  - src/services/providers/gemini.provider.js
  - src/services/providers/index.js
  - src/core/config.js
  - main.js
  - preload.js
  - src/ui/main-window.js
  - settings.html
  - src/ui/settings-window.js
  - onboarding.html
  - onboarding.js
  - test/gemini-request-parity.test.js
  - test/fixtures/gemini-requests/text.json
  - test/fixtures/gemini-requests/image.json
  - test/fixtures/gemini-requests/transcription.json
  - scripts/capture-gemini-goldens.js
requirements: [PROV-07]
must_haves:
  truths:
    - "The deletion runs ONLY after an explicit human approval that Local is proven — it never auto-deletes on criteria pass"
    - "Gemini is fully removed: the @google/genai SDK, the Gemini provider (incl. its cert-verify bypass + UA override), the Gemini IPC handlers + preload bridges, the orphaned Gemini modal, the llm.gemini config block, and the Gemini golden fixtures/tests/capture script"
    - "Azure STT is deliberately KEPT this phase (the SDK + the browser-DOM polyfill + Azure settings/onboarding) — voice still works end to end"
    - "The app still answers on Local after removal; the provider switcher UI remains (Local only) so Phase 7 CLI providers slot in with no rework"
    - "The Gemini cert-verify bypass is gone with zero dead global startup code (LocalProvider defines no configureNetworkSession, so the guarded delegate in setupNetworkConfiguration simply no-ops)"
  artifacts:
    - path: "src/services/providers/index.js"
      provides: "registry with local only; getSelected falls back to local"
      contains: "local"
    - path: "package.json"
      provides: "@google/genai removed; microsoft-cognitiveservices-speech-sdk KEPT"
      contains: "microsoft-cognitiveservices-speech-sdk"
  key_links:
    - from: "main.js setupNetworkConfiguration()"
      to: "provider.configureNetworkSession (now undefined on LocalProvider)"
      via: "guarded delegate no-ops → cert bypass vanishes"
      pattern: "configureNetworkSession"
    - from: "src/services/providers/index.js getSelected()"
      to: "this.providers.local"
      via: "fallback after gemini removed"
      pattern: "providers\\.local"
---

<objective>
Fully remove the Gemini LLM path (PROV-07 / SC5) — the irreversible "burn the boats" step — as the LAST plan, gated behind a HARD MANUAL CHECKPOINT that only proceeds on the user's explicit approval after Local is proven (03-07). Removal scope is **GEMINI ONLY**; Azure STT is deliberately kept.

Purpose: This is the final step of the load-bearing sequencing (abstraction → Local BUILT → Local PROVEN → cloud removed LAST). The Phase-2 seam already relocated the cert bypass into the provider and made `setupNetworkConfiguration` a guarded delegate, so removal is clean.

**Documented SC5 divergence (intentional):** SC5 literally says "remove … the Azure browser-DOM polyfill." RESEARCH Flag 4 confirmed Azure is **STT-only** and the polyfill (`src/services/speech.service.js:1–~380`) is inseparable from the Azure Speech SDK — removing it breaks voice. The higher-priority locked decision "keep STT working throughout" wins: the Azure SDK + polyfill + Azure settings/onboarding stay, and their removal is DEFERRED to Phase 4 (when resident whisper.cpp replaces the whole STT layer). This plan removes Gemini only; the SUMMARY must record this deliberate divergence.

Output: Gemini deleted; app runs on Local alone; Azure STT intact; tests + lint green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-local-engine-cloud-removal/03-CONTEXT.md
@.planning/phases/03-local-engine-cloud-removal/03-RESEARCH.md
@src/services/providers/gemini.provider.js
@src/services/providers/index.js
@main.js
@preload.js
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: HARD MANUAL APPROVAL — authorize the irreversible Gemini deletion</name>
  <action>
STOP and require an explicit human approval before ANY deletion. This is a hard manual gate: present the exact deletion scope (below), confirm 03-07 signed off "approved" (Local proven), and WAIT for the user to type "approved". Tasks 2–3 run ONLY on "approved". On "defer" (or anything else), stop and change nothing — never auto-delete on criteria pass; never remove Gemini before Local is proven (Pitfall 12).
  </action>
  <decision>Approve permanently deleting the Gemini LLM path now? This is irreversible in this commit. Do NOT proceed without an explicit "approved".</decision>
  <context>
Precondition: 03-07 signed off "approved" (Local is PROVEN — all 3 on-demand entry points answer locally + the rough TTFT/memory smoke passed). If 03-07 did not pass, STOP.

This deletes exactly the following (GEMINI ONLY — reviewable scope):
- `@google/genai` dependency (npm uninstall).
- `src/services/providers/gemini.provider.js` (whole file — includes the cert-verify bypass + UA override in `configureNetworkSession` and the behavioral keyword list).
- Gemini registration in `src/services/providers/index.js` (registry becomes Local-only; `getSelected()` falls back to Local).
- The `llm.gemini` block in `src/core/config.js`.
- Gemini IPC handlers in `main.js`: `set-gemini-api-key`, `get-gemini-status`, `test-gemini-connection`, `run-gemini-diagnostics`; the `geminiKey` fields in getSettings/saveSettings + the Gemini-key reinit block.
- Gemini preload bridges: `setGeminiApiKey`, `getGeminiStatus`, `testGeminiConnection`, `onOpenGeminiConfig`.
- The orphaned Gemini modal in `src/ui/main-window.js` (`showGeminiConfig`/`createGeminiConfigModal`/`configureGemini`).
- The Gemini "API key" section in `settings.html` + its refs in `src/ui/settings-window.js`; the Gemini `apikey` onboarding screen in `onboarding.html`/`onboarding.js`.
- The Gemini goldens + parity test + capture script: `test/gemini-request-parity.test.js`, `test/fixtures/gemini-requests/*`, `scripts/capture-gemini-goldens.js`.

This deliberately does NOT touch (Azure STT — deferred to Phase 4): `microsoft-cognitiveservices-speech-sdk`, `src/services/speech.service.js` (incl. the browser-DOM polyfill), the Azure settings/onboarding fields, and the Azure plumbing in main.js getSettings/saveSettings. Voice must keep working.
  </context>
  <options>
    <option id="approved">
      <name>approved — proceed with the irreversible Gemini deletion</name>
      <pros>Completes SC5 (Gemini removed, done last); deletes the cert bypass + hardcoded hosts + orphaned modal cleanly.</pros>
      <cons>Irreversible in this commit; Local becomes the sole engine (recovery UX from 03-06 is the safety net until Phase 7 CLI backups).</cons>
    </option>
    <option id="defer">
      <name>defer — stop; keep Gemini selectable</name>
      <pros>Keeps the cloud comparison path if Local isn't trusted yet.</pros>
      <cons>Phase 3 SC5 not met; revisit after more validation.</cons>
    </option>
  </options>
  <verify>An explicit "approved" was received AND 03-07 is signed off (Local proven). Absent either, no files are changed.</verify>
  <done>The user has explicitly authorized (or deferred) the irreversible deletion; deletion tasks proceed only on "approved".</done>
  <resume-signal>Type "approved" to authorize deletion (Tasks 2–3 run only then), or "defer" to stop without deleting.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Delete the Gemini engine — SDK, provider, registry, config, goldens</name>
  <files>package.json, package-lock.json, src/services/providers/gemini.provider.js, src/services/providers/index.js, src/core/config.js, test/gemini-request-parity.test.js, test/fixtures/gemini-requests/text.json, test/fixtures/gemini-requests/image.json, test/fixtures/gemini-requests/transcription.json, scripts/capture-gemini-goldens.js</files>
  <action>
Runs ONLY after Task 1 = "approved". Delete the Gemini engine (RESEARCH Flag 4 "SAFE to delete"):

1. `npm uninstall @google/genai` (updates package.json + package-lock.json). Do NOT uninstall `microsoft-cognitiveservices-speech-sdk`.
2. Delete the file `src/services/providers/gemini.provider.js` (its cert/UA bypass + hardcoded `generativelanguage.googleapis.com` hosts + behavioral keyword list all go with it).
3. `src/services/providers/index.js`: remove `require('./gemini.provider')`, `const gemini = new GeminiProvider()`, and `gemini` from the `providers` map. Registry becomes `{ providers: { local }, selected: config.get('llm.provider'), register, get, getSelected }`; update `getSelected()` fallback to `return this.providers[this.selected] || this.providers.local;` (Local is the only provider now; an unknown/`gemini` selection resolves to Local — graceful).
4. `src/core/config.js`: remove the `llm.gemini` block. `llm` becomes `{ provider, local }`. Leave the `speech.*` block (incl. any dead `speech.provider`) untouched — Azure STT is a separate Phase-4 concern.
5. Delete the Gemini test/goldens: `test/gemini-request-parity.test.js`, the three `test/fixtures/gemini-requests/*.json`, and `scripts/capture-gemini-goldens.js`. (These embedded the interview prompt GEN-01 changed; they retire with Gemini as planned.)

Do NOT edit `main.js setupNetworkConfiguration()` (main.js:289): it already guards on `configureNetworkSession` existing; LocalProvider defines no such method, so the cert bypass vanishes with zero dead global startup code (verify in Task 3).
  </action>
  <verify>
`grep -RIl "google/genai\|GeminiProvider\|generativelanguage" src/ test/ scripts/ | grep -v node_modules` returns nothing. `test -f src/services/providers/gemini.provider.js` is false (deleted). `node -e "console.log(require('./src/services/llm.service').getStats().provider)"` prints `local`. `grep -n microsoft-cognitiveservices-speech-sdk package.json` still present (Azure kept). `npx eslint .` exits 0.
  </verify>
  <done>@google/genai + the Gemini provider/registry/config/goldens are gone; the facade resolves Local; Azure SDK kept; no Gemini symbols remain in src/test/scripts.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the Gemini IPC/UI surface + verify Local-only + Azure intact</name>
  <files>main.js, preload.js, src/ui/main-window.js, settings.html, src/ui/settings-window.js, onboarding.html, onboarding.js</files>
  <action>
Runs ONLY after Task 1 = "approved". Remove the Gemini IPC + UI surface, then verify the app is clean:

1. **main.js**: remove the IPC handlers `set-gemini-api-key` (~583), `get-gemini-status` (~588), `test-gemini-connection` (~618), `run-gemini-diagnostics` (~622). Remove `geminiKey` from `getSettings()` (~1487) and from `saveSettings()` envUpdates (~1547-1549) + the "reinitialize after Gemini key" block (~1564-1572). Keep the local-named IPC (`test-provider-connection`, `get-model-status`, etc. from 03-04). Keep ALL Azure/whisper plumbing (SPEECH_PROVIDER/AZURE_SPEECH_KEY/AZURE_SPEECH_REGION in getSettings/saveSettings) untouched.
2. **preload.js**: remove `setGeminiApiKey`, `getGeminiStatus`, `testGeminiConnection` (~34-37) and `onOpenGeminiConfig` (~112). Keep the model/local bridges + all speech bridges.
3. **src/ui/main-window.js**: remove the orphaned Gemini modal methods `showGeminiConfig`/`createGeminiConfigModal`/`configureGemini` (~1004-1096). Keep the Local-down recovery UI (03-06).
4. **settings.html**: remove the "Gemini Settings" section (the `#geminiKey` block). Keep the Provider picker (now Local only — leave Gemini out of the `#llmProvider` options; the minimal switcher stays for Phase 7). Keep Azure + whisper sections.
5. **src/ui/settings-window.js**: remove `geminiKeyInput` ref (~16), its load (~84), save (~140), and its entry in the `inputs` array (~185). Remove the `gemini` option handling from the provider picker (Local only). Keep everything else.
6. **onboarding.html / onboarding.js**: remove (or neutralize) the Gemini `apikey` screen (`data-screen="apikey"`, onboarding.html:742) and drop `apikey` from the onboarding screen order + its handlers in onboarding.js. Keep the Ollama/model-pull screens (03-06) + the speech screen.

**Verify (the removal is clean + safe):**
- `grep -RIn "gemini" main.js preload.js src/ui/ settings.html onboarding.html onboarding.js` (case-insensitive) returns nothing. (setupNetworkConfiguration keeps its guarded delegate but no provider defines configureNetworkSession now — confirm LocalProvider has no such method.)
- Azure STT still present (`grep microsoft-cognitiveservices-speech-sdk package.json`; speech.service.js untouched).
- `node --test test/*.test.js` all green (the deleted gemini-parity test is gone). `npx eslint .` exits 0.
- Boot smoke: `npm start` launches, answers a question on Local, and STT/mic still initializes.
  </action>
  <verify>
`grep -RIn "gemini" main.js preload.js src/ui/ settings.html onboarding.html onboarding.js` (case-insensitive) returns nothing. `grep -n configureNetworkSession src/services/providers/local.provider.js` returns nothing (cert bypass truly gone). Azure STT still present (`grep microsoft-cognitiveservices-speech-sdk package.json` matches; speech.service.js untouched). `node --test test/*.test.js` green; `npx eslint .` exits 0. `npm start` boots on Local and voice still initializes.
  </verify>
  <done>Gemini IPC/preload/modal/settings/onboarding surface removed; app runs on Local only with the minimal provider switcher retained; cert bypass gone with no dead startup code; Azure STT fully intact.</done>
</task>

</tasks>

<verification>
- Deletion happened ONLY after explicit "approved" (Task 1 gate); never auto-deleted on criteria pass.
- Gemini fully removed (SDK, provider+cert bypass, IPC+preload, modal, config, goldens/test/capture) — `grep -RIn gemini src/ main.js preload.js settings.html onboarding.*` clean.
- Azure STT deliberately KEPT (SDK + polyfill + settings/onboarding) — documented SC5 divergence (Phase 4).
- Full-sustained validation divergence (Phase 6) already documented in 03-07.
- App runs on Local alone; provider switcher retained (Local only) for Phase 7; `node --test test/*.test.js` green; `npx eslint .` exits 0; `npm start` boots + voice works.
</verification>

<success_criteria>
PROV-07 / SC5 met at the Phase-3 scope: Gemini fully removed behind a hard manual approval, done LAST after Local was proven; Azure STT retained with the divergence documented; the app is a working local-first engine.
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-engine-cloud-removal/03-08-SUMMARY.md` recording: the approval sign-off, the exact deletion set, confirmation the cert bypass vanished with no dead code, the Azure-STT-kept divergence (Phase 4) and the full-sustained divergence (Phase 6), and the green post-removal boot + tests.
</output>
