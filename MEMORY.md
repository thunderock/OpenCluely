# Project Memory — OpenCluely

<!-- Per-repo durable facts. Global memory: ~/.claude/memory/ (Claude) / inlined in ~/.codex/AGENTS.md (Codex). -->
<!-- Keep this SHORT — only what's specific to THIS repo. Entries: `YYYY-MM-DD — what — why`. No secrets. -->

## What this is
Electron app being transformed into a local-first, always-on multimodal copilot (GSD-planned, 9 phases; see `.planning/`). Cloud Gemini REMOVED in Phase 3; Azure STT + its browser-DOM polyfill REMOVED in Phase 4 — the app is now fully local (Ollama LLM + resident whisper.cpp STT, ambient listening). Phases 1–4 complete & merged; next is Phase 5 (continuous capture, notes, hardening).

## Local facts
- 2026-07-14 — Install deps with `npm ci --ignore-scripts` — plain `npm ci` (what `make setup`/`setup-dev` run) triggers the electron-builder postinstall that downloads the ~100 MB Electron binary; slow/destructive in sandboxed contexts.
- 2026-07-14 — Run tests with `node --test test/*.test.js` (glob) — the bare-directory form `node --test test/` fails to resolve on newer Node. Makefile `run_tests` and CI use the glob.
- 2026-07-14 — Lint gate is `npx eslint .` (ESLint 9 flat config `eslint.config.js`), error-only, must exit 0. `make lint` wraps it.
- 2026-07-14 — Pure, testable logic lives in `src/core/*` (env-file, skill-normalizer, vad-segmenter, service-supervisor); the god-file singletons (`src/services/speech.service.js` etc.) delegate to them. Tests must import `src/core/*`, never the singletons.
- 2026-07-15 — LLM path (post-Phase-3, Gemini REMOVED): `main.js` → `src/services/llm.service.js` (17-line facade = `require('./providers').getSelected()`, NOT destructured — that loses `this`) → `LocalProvider` (`src/services/providers/local.provider.js`) → `RequestBuilder` (`src/core/request-builder.js`, pure/DI, input-neutral struct) → `serialize()` → **openai SDK over Ollama `/v1`** (`baseURL=host+"/v1"`, `apiKey:"ollama"`). Registry (`providers/index.js`) is Local-only; `getSelected()` falls back to `local`. Phase 7 adds CLI providers behind the same registry.
- 2026-07-15 — qwen3-vl:8b OVER-REASONS: it ignores the Ollama API `think:false` param; the `/no_think` soft-switch in the SYSTEM prompt (`LocalProvider`, GEN-01 default-on) suppresses `<think>` on SIMPLE prompts, but on heavy bounded-notes+image prompts it still emits hundreds of reasoning tokens before content → slow time-to-first-content. OPEN Phase-6 decision: accept vs. switch default to a non-reasoning curated model (`gemma3:4b/12b`). `scripts/smoke-local.js` measures the answer over `/v1` (app path) + prefill over `/api/chat`; `SMOKE_NUM_PREDICT` tunable.
- 2026-07-15 (updated 2026-07-16) — Electron-main network RULE (`src/core/local-transport.js`): the ambient `fetch` is Chromium-net-backed and false-negatives loopback daemons Node http reaches fine — never use it for loopback health probes in the Electron main process; use `nodeFetch` (WHATWG fetch over Node http) or `ServiceSupervisor.probeHttp`. Applies to Ollama AND whisper-server. Don't block a `serverUp` status poll on a full model `generate` — `_modelResponds` is a cheap bounded ping (`think:false` + `num_predict:1` + hard timeout). (The polyfill half of this entry is obsolete: the Azure browser-DOM polyfill was DELETED in Phase 4 and `ensureNativeGlobalURL()` retired — `global.URL` is never clobbered anymore; `nodeFetch` stays.)
- 2026-07-15 — Gemini is GONE (PROV-07): `gemini.provider.js` + `@google/genai` + the gemini-parity test/fixtures/capture script deleted; `main.js setupNetworkConfiguration()` is now a guarded no-op (LocalProvider defines no `configureNetworkSession`, so the cert-verify bypass vanished with no dead code). Residual Gemini COPY still lingers in `setup.sh`/`README.md`/`release.yml`/`webapp/` (non-runtime) → Phase 8/9.
- 2026-07-16 — Azure STT is GONE (STT-05, Phase 4): SDK (`microsoft-cognitiveservices-speech-sdk`) uninstalled, the ~380-line browser-DOM polyfill deleted (`speech.service.js` 1648→918L), `speech.provider`/`speech.azure` config + azure UI/env removed, `ensureNativeGlobalURL()` fully retired (it had FOUR call sites — the 4th added by 04-01 after the removal plan was written). `node-record-lpcm16` KEPT (Linux mic, not Azure). Marketing copy `webapp/index.html` still says Azure → Phase 9.
- 2026-07-16 — STT path (post-Phase-4): renderer `getUserMedia` → `audio-chunk` IPC → `SpeechService` TWO independent per-channel pipelines (`_channels.mic/.system`, each own VadSegmenter + in-flight serialization) → three-gate filter (JS VAD → whisper `no_speech_prob>0.6` → `_isHallucinatedTranscript`) → `WhisperServerManager.transcribe()` = POST `/inference?response_format=verbose_json` (the ONLY whisper-server format exposing `no_speech_prob`) → `{text, source:'mic'|'system'}` tagged through to `sessionManager.addUserInput(..., {channel})`. whisper-server = from-source whisper.cpp build at `resources/bin/whisper-server` (Metal), supervised by `ServiceSupervisor` (own, ephemeral port re-picked each start); model `ggml-small.en` in `<userData>/.whisper-models/`. Ambient listening auto-starts launch→quit (`_ensureAmbientListening`, defers until engine ready); sleep/wake re-warm via pure `src/core/wake-rewarm.js`; AirPods swap re-attach via `devicechange` + `speech-reattach-channel` IPC.
- 2026-07-16 — Deferred to Phase 8 (human-approved, in `.planning/phases/04-*/deferred-items.md`): the system-audio SIGNING SPIKE (macOS TCC `NSAudioCaptureUsageDescription` prompt won't fire on unsigned builds — Core Audio Process Tap capture is IMPLEMENTED + degrades-to-mic but NOT verified live) and the attended real-world validation (real 488MB model download, 2-min silence, real sleep/wake + AirPods). Don't claim system-audio capture "works" until the Phase-8 signed-build spike passes.

## Gotchas
- 2026-07-14 — gsd-tools CLI can't parse this repo's NARRATIVE ROADMAP/STATE format — `phase complete` / `state advance-plan` / `update-progress` silently no-op AND `phase complete` falsely reports `is_last_phase: true`. Mark phase/plan/requirement completion MANUALLY in ROADMAP.md + REQUIREMENTS.md; verify next-phase via ROADMAP, not the CLI's `is_last_phase`.
- 2026-07-14 — Parallel GSD executors share one branch/worktree: a bare `git commit` after `git add <file>` commits the whole staged index and can sweep a sibling's staged files into your commit (mislabeling attribution — see `510a7da` labeled feat(01-03) but containing 01-01 files). Use explicit-pathspec commits: `git commit -- <files>`.

## Workflow
- 2026-07-13 (updated 2026-07-16) — Merge every phase to `main` when complete, BEFORE planning/branching the next phase, so phases don't entangle on one branch (next phase branches off freshly-merged main). CLAUDE performs the merge itself — it's Claude's responsibility at phase-complete (Ashutosh clarified 2026-07-16; the entry previously said he did it). Pushing + PRs remain Ashutosh's alone — never auto-push. (Lesson: Phase 2 was planned on the unmerged Phase 1 branch — entangled.)
- 2026-07-13 — After each phase merges to `main`, capture that phase's durable learnings into BOTH global memory (`~/.claude/memory/`; source: `ashutosh_setup/setup/memory/`) and this file. Claude merges phases locally; Ashutosh handles all GitHub pushes + PRs himself — never auto-push/PR.
